# التسليم — إلى المحادثة التالية

> **التاريخ:** ٢٠٢٦-٠٩-١٢ · **الفرع:** `fix/production-hardening` · **HEAD وقت الكتابة:** انظر §١
> **هذا الملفّ متتبَّع في المستودع عمداً** — سابقُه (`HANDOFF-2026-09-10.md`) غير متتبَّع،
> فكان كلّ استشهادٍ به يصحّ على قرصٍ واحد ولا يصحّ لغيره. لا تُكرّر ذلك.

---

## ٠ — قبل أيّ سطر

1. **الميثاق مُلزِم**: `CLAUDE.md` في الجذر. **لا يُعدَّل** — تعديله قرارُ المالك وحده.
2. **الردّ بالعربية**، والمصطلحات والمسارات بالإنجليزية.
3. **`node scripts/run-gate-wave0.mjs` يخرج بـ0 قبل كلّ commit.** بلا استثناء.
4. **ما لا يُستردّ يُطلب في كلّ مرّة**: الدفع · النشر · إتلاف بيانات أو تاريخ ·
   أيّ مسٍّ بحسابٍ حقيقي. **وغياب المالك لا يوسّع الحدّ.**

   > **وإذنٌ مُنح في محادثة ٢٠٢٦-٠٩-١٢ — يُسجَّل بحدوده لا بسعته.** نصّه: «اكمل اي عمل
   > متوقف على قراري او اذني وانا اقول لك امنحك الاذن بشرط الحيطة والحذر والاحترافية»،
   > ثمّ: «ان كان الامر خطورته عاليه او تاثيره سلبي اكثر مو كونه ايجابي او يفضل تركه
   > فانت صاحب القرار». فهو إذنٌ **مشروطٌ بالحكم لا بديلٌ عنه**: يُلزم بالترك حين يرجح
   > الضرر، لا بالفعل حين يُمكن.
   >
   > **ولا يُفترض سريانه إلى محادثةٍ جديدة.** مُنح هنا لعملٍ بعينه؛ وامتدادُه قرارُ
   > المالك يُسأل عنه، لا يُستنتج من هذا السطر. وما استُعمل به مكتوبٌ في رسائل
   > الالتزامات نفسها — `baseline(hq)` يقتبسه حرفياً كما يشترط §٤.
5. **التجميد البصريّ**: لا `className`، لا `style={{}}`، لا CSS، لا ألوان/تباعد/خطوط
   بلا إذنٍ حرفيّ. وكلّ commit يحمل `Zero Visual Edits = confirmed`.
6. **قِس قبل أن تُصلح، وسمِّ العطل الذي يمنعه تغييرك.** ما لا تستطيع تسمية عطله لا يُشحن.

---

## ٠أ — ما تغيّر **بعد** كتابة هذا الملفّ (جلسة ٢٠٢٦-٠٩-١٢ مساءً)

ثلاثة التزامات، لكلٍّ بوّابتُه الخضراء على شجرته وحدها (٣١/٣١ · ٧٨١ث · ٧٦٦ث · ٩١٧ث):

| الالتزام | ماذا |
|---|---|
| `e1c4deb9` | `docs(audit):` تثبيتُ قياس متصفّح المالك الذي كان معلّقاً بلا التزام |
| `63765c61` | `fix(security-drill):` **الخمسة الأمنية أُغلقت** |
| `4f6c09fc` | `feat(guard):` حارسُ الخاصّية التي كشفتها حمرتُها |

**والخمسة الأمنية لم تكن عن Linux ولا عن اختيار مخزن.** `resetWifeDrillEnv` كان يُعلن
`SUPABASE_URL` و`SUPABASE_SERVICE_ROLE_KEY` ويسكت عن `SUPABASE_ANON_KEY`، فيستعيره من
`.env` المطوِّر محلياً ولا يجده العدّاء. والمستهلِك — مُسمّىً بمسبارٍ مُعترِض لا بقراءة:

```
sessionCookie.ts:103-107    يشترط URL **و** anon، بلا رجوعٍ إلى المفتاح المُمتاز
wifeTokenSubject.ts:95-96   cfg === null  ⇒  return null **قبل** أيّ fetch
wifeValidator.ts:28 · wifeCsrfVerify.ts:8   كلاهما يمرّ بـ getVerifiedTokenSubject
```

**وتصحيحان لتقرير المراجعة (§٢ · F9) — كلاهما في الآليّة لا في النتيجة:**

1. `wifeNonceStore.ts:57` **ليس** المسار: فيه `||` فهو صادقٌ بالمفتاح المُمتاز وحده.
2. `vite.config.mts:85` **لا يعمل تحت vitest**: هو داخل `attachApiRouteMiddleware`،
   ولا يُستدعى إلا من خطّاف `configureServer` (`:272`)؛ و`:1754` يُحمّل إلى متغيّرٍ
   محلّي بلا `Object.assign`. **مَن يحقن `.env` هو vitest نفسه.**

وبهما ينحلّ التناقض المسجَّل في `FINDING_SECURITY_SUITE_CI_ONLY.md` (حذفان مختلفان
يُنتجان الخمسة نفسها): **حذفُ `SUPABASE_URL` يُبطل النصف الآخر من الشرط ذاته** — دالّةٌ
واحدة بشرطين، لا حسّاسيةٌ عامّة.

**القياس** (في `git worktree` بلا `.env`، وهو شرطُ العدّاء): ٥/١١٠ → **١١٠/١١٠** ·
و`npm run test:security` ٥/٣٣٠ → **٣٣٣/٣٣٣**. وشاهدٌ بمتغيّرٍ واحد: كتابةُ
`SUPABASE_ANON_KEY` وحده في `.env` هناك — بلا تعديل شفرة — تقلب الخمسة إلى صفر.

**وفخٌّ يُكتب لئلّا يُعاد:** متغيّرُ الصَّدَفة **لا يغلب** `.env` تحت vitest (قيس مرّتين).
فلا سبيل إلى محاكاة العدّاء من الصَّدَفة؛ السبيلُ الوحيد شجرةٌ بلا `.env`.

### وما بقي مفتوحاً من هذه الجلسة

- **الدفع لم يقع.** الفرع ٢٢١ أمام `origin/main` ولم يُؤذن بالدفع في هذه المحادثة.
  *(أُذن به لاحقاً ووقع — انظر §٠ب.)*
- **`FINDING_STORAGE_CLOUD_COVERAGE` و`FOUNDATION_MAP` و§٣ بند ٣ هنا** ما زالت تقول إنّ
  الشريحة السادسة «**تُتلف** النسخة القائمة»، وقد أسقطته F6 بالقياس (`workCloudCheckpoint.ts:351-367`
  يرفض **قبل** `POST`). **معطى قرارٍ للمالك ما زال خاطئاً في ثلاثة مواضع** — يُصحَّح
  بالتزام `fix(audit):` مستقلّ. *(نُفّذ في `8fca390c` — والمواضع كانت أربعة لا ثلاثة.)*

---

## ٠ج — جلسة ٢٠٢٦-٠٩-١٣ (ليلاً): ما أُغلق، وما بقي بمالكه

**الحدث:** تشغيلة العدّاء **#43** على `30b4d594` **خضراء بالكامل، وظيفتين معاً** —
ولأوّل مرّة في تاريخ المستودع عملت الخطوة ٤٨ (مجموعة الاختبارات الأمنية) ووظيفة
`e2e-boot-fail-closed`، وكلتاهما مرّت.

    https://github.com/onebest993-wq/hami0.5/actions/runs/34723516259

**والسبب أنّهما كانتا محجوبتين لا معطّلتين:** الخطوة ٤٧ (`Bundle size budgets hold`)
كانت تسقط، فتتخطّى الوظيفةُ كلَّ ما بعدها. وسقوطُها كان كذباً: الحارس يلتقط المدخل
بـ`/index-*/` **ومدخل هذا المشروع `main-*`**، فيسقط إلى حزمةٍ تافهة (٢٫٤ ك.ب) ويقيس
إغلاقها — ١٠٧ ملفّات · ٧٤٣ ك.ب مضغوطة مقابل حدٍّ ٣٢٠. **والمسار الحقيقيّ ٩٧ ك.ب.**

### الالتزامات الستّة في هذه الجلسة (كلٌّ ببوّابةٍ خضراء على شجرته)

| | ما فيه |
|---|---|
| `30b4d594` | حارس ميزانية الإقلاع يقيس المدخل الصحيح · **فُتحت به ٤٨ ووظيفة E2E** |
| `f35b7905` | حدُّ `docs(` مفروضٌ آلياً + ٨ أسنانٍ صندوقاً أسود (٣ تسقط على HEAD السابق) |
| `c3c63a03` | `.gitattributes` — **صفرُ بايتٍ متغيّر**، مُثبتاً بشجرة عملٍ معزولة |
| `60d93cb2` | أربعةُ مصادرَ كانت ثنائيةً عند git → نصّاً، بلا تغيير سلوك |
| `eb495ab3` | فحصُ ترميز الرسالة (BOM + محارف التحكّم) + تصحيحُ صفٍّ كاذب في §٣ |

### أربعةٌ كانت لك وحدك — ثلاثةٌ أُغلقت والرابع ينتظر كلمةً

1. **`79315b66`** ✅ قِيس فتغيّر وصفه: الأربعة `R100` — **إعادةُ تسميةٍ خالصة** (نقلُ
   طابق T21) لا شفرةٌ جديدة. سُجّل في `GRANDFATHERED` **مقيَّداً بقاعدة `DOCS_SCOPE`
   وحدها**، فبادئتُه وعبارةُ التجميد وخطوطُ الأساس تُفحص كما تُفحص لغيره.
2. **`.gitattributes`** ✅ **والدَّين لم يكن موجوداً:** ولا ملفَّ واحدٍ **فهرسُه** `crlf`
   أو `mixed` (١٠٬٣٥٢ ملفّاً). فانقلب العمل من إعادة تسويةٍ واسعة إلى قفلِ ما هو صحيح.
3. **شكل مزامنة الجزائي** ✅ قرارٌ مكتوب: **صفٌّ لكلّ قضية عبر BFF**، لا شريحةً سادسة —
   `5 × 262,144 = 1,310,720 > 1,300,000`. وثلاثةُ أسئلةٍ تنتظر كلمتك في §٦ منها.
4. **الدمج في `main`** ⏸ **موقوفٌ على حقيقةٍ واحدة لا أقيسها من هنا** — انظر أدناه.

### الدمج: ما قِيس، وما لم يُقَس

    الفرع أمام origin/main بـ**٢٤٦ التزاماً**؛ ورأسُ main مؤرَّخٌ ٢٠٢٦-٠٨-٣١
    الدمج **تقديمٌ سريع** نظيف (main سلفٌ لـHEAD) — بلا تعارضٍ واحد
    مشروع Vercel **موصولٌ فعلاً**: .vercel/project.json → prj_AOIwXVGxWE2ZWI5oIOTldauxwE71
    ولا سير عملٍ في المستودع ينشر عند الدفع إلى main (مسحُ الخمسة)

**والمجهول:** «فرعُ الإنتاج» في Vercel إعدادٌ في لوحتهم، وافتراضُهم هو الفرع الافتراضيّ —
أي `main`. **والبند الأوّل من التخويل يوجب كتابة «مَن يراه» قبل الفعل**، وهذا ما لا
أملكه. ولو كان الجواب «يُنشر»، فدفعُ عملِ أسبوعين غيرِ مُراجَعٍ إلى تطبيقٍ يستعمله
محامون **يمسّ طرفاً ثالثاً** — الحدُّ الوحيد الباقي فوق كلّ تخويل.

> **وفخٌّ يخصّ من ينفّذ الدمج:** `main` المحلّيّ (`2dfdda91`، ٢٠٢٦-٠٩-٠٦) **ليس**
> `origin/main` (`96365741`، ٢٠٢٦-٠٨-٣١). فمن يدمج بـ`git checkout main` يدمج في فرعٍ
> غير الذي يظنّه. استعمل `origin/main` صراحةً.

### ثلاثةُ اكتشافاتٍ عرضية — مقيسةٌ ومتروكةٌ بسبب

| الاكتشاف | القياس | لماذا تُرك |
|---|---|---|
| `public/debug-sample.pdf` **مكسورٌ على كلّ جهاز ويندوز** | إزاحةُ `startxref` تقع داخل قاموس خطّ بدل `xref` | **أُصلح** — نسخةُ العمل استُعيدت من الفهرس |
| **٦ حزم قوانين يتيمة، ٩٧٥ ك.ب** تُشحن للإنتاج | ١٥ على القرص · ٩ في المانيفست · ٠ مفقود · لا مرجع خارج `.audit/` | `sw.js` يُخزّن **بالنمط لا بالاسم**، فعميلٌ بمانيفستٍ قديمٍ في ذاكرته يطلب اسماً محذوفاً — **قرارُ مدّةِ إبقاءٍ لا تنظيف** |
| **حزمةُ القوانين تُسكّ باسمٍ مختلف — وقد وقع فعلاً لا توقّعاً** | قياسُ `section-gates` **غيّر ملفّاتٍ متتبَّعة**: `manifest.json` صار يشير إلى `execution.15b069a06d85.json` بدل `192daa4e7c3a` المتتبَّع | الإصلاح سطر (تسويةُ المحتوى قبل التجزئة) — **ولا يُستعجَل في بيانات القوانين آخر الليل** |

> **وتحذيرٌ لمن يُشغّل `section-gates` بعدي:** بوّابةُ `core-boot` **تُعيد توليد حزم
> القوانين**، فتُعدّل `public/static-law-data/manifest.json` وتسكّ ملفّاً جديداً. أي
> أنّ تشغيلها **يُوسّخ شجرةً نظيفة**. نُظّفت هنا بـ`git checkout --` وحذفِ الملفّ
> الجديد؛ ومن نسي ذلك يُدخل حزمةً مُولَّدة في التزامٍ لا علاقة له بها.

### فخّان جديدان لهذا الجهاز — يُضافان إلى §٤

1. **`Get-Content -Raw` في PowerShell 5.1 يفكّ UTF-8 على أنّه ANSI.** أيّ
   `(Get-Content -Raw) -replace … | WriteAllText(UTF8)` **يُضاعف الترميز**. أنتج رسالةَ
   التزامٍ من ٣٧١ بايتاً لا تُقرأ؛ أُمسكت قبل الدفع. **ووحدةُ التحكّم تكذب في
   الاتّجاهين** — يُقاس بـ`node -e "…execFileSync('git',['log','-1','--format=%s']).length"`.
   وصار محروساً في `guard:commit-conventions`.
2. **كتابةُ هروبٍ نصّيّ داخل حمولة JSON تُنتج البايت الحقيقيّ.** كتابةُ الهروب الصفريّ
   في نصّ رسالةٍ أنتجت بايت NUL فعلياً، ورفضه git صراحةً. **يُوصف المحرف بالكلام.**

### ٠ج·١ — وثلاثةُ نواقصَ سلّمتُ العمل وأنا أعرفها، فأُتمّت

سلّمتُ ثمّ عرضتُ ثلاثة نواقص **سؤالاً**. والمالك ردّ بأنّ الإتمام التزامٌ ذاتيّ لا
يُستأذن فيه — فعُدّل الميثاق (dcbc4b92، §٧ المرحلة ٤: **الحلُّ الرخيص عيبٌ لا علاج**،
**والقياسُ المعلَّق يُحسم**، **وما أدخلتَه أنت تُصلحه أنت**) ثمّ أُتمّت الثلاثة:

| النقص | ما صار |
|---|---|
| سمّيتُ خمس بوّابات «تخضرّ بلا أن تعمل» ولم أقِس علاجه | قِيست الأعلام: `gate:hq -- --tests --build` و`gate:wife-production -- --tests` تمرّان فمُرِّرا؛ و`gate:auth-onboarding -- --tests` يسقط على **فشلٍ مقبولٍ في خطّ الأساس** فامتنع بسببٍ مكتوب |
| core-boot: «لا أزعم أنّها تسقط ولا أنّها تمرّ» | **حُسم:** العيّنة الأولى تدفع كلفةَ البدء الباردة (٦٩٤ → ٣٥٣ → ٣٢٣)، وp95 على ثلاثٍ هو الأقصى. عيّنةُ إحماءٍ تُهدَر → p95 **٦٩٤ ← ٣٦٦**، والجوّال صار يمرّ. ويبقى ١٦م.ث **قرارُ مالك** |
| أثرُ .gitattributes على تجزئة حزم القوانين | **أُصلح:** التسوية قبل التجزئة. والقياس صحّح وصفي: «٦ يتامى» كان **٥ منها توائمَ LF**، و٩ من ٩ في المانيفست مُشتقّةٌ من CRLF |

**ودرسٌ يخصّني:** الثلاثة كانت معروفةً لي ساعةَ سلّمت. **والسؤال عنها لم يكن حيطةً بل
تأجيلاً**، وهو ما أغلقه البند الجديد.

---
### ما بقي مفتوحاً بعد هذه الجلسة

- **الدمج** — ينتظر كلمتك أعلاه.
- **`section-gates.yml`** — قِيست الستّ عشرة محلّياً (النتيجة في `.audit/`)؛ وهو ساكنٌ
  حتى يُدمج، وتفعيلُه جزءٌ من قرار الدمج.
- **سبعةُ اختباراتٍ** في خطّ الأساس · **قياسُ تكرار الوحدات بين الحزم** (مصمَّم، غير
  مشحون) · **حارسُ البوّابة المتناسبة** الذي يُفعّل بند §٣.

---

## ٠ب — الحصاد: ما قاله العدّاء حين عمل لأوّل مرّة

**الحدث:** الخطوة ٣٧ (`guard:tests`) في `quality-gate.yml` صارت **خضراء على العدّاء
لأوّل مرّة**. فعملت الخطوات ٣٨ فما بعد — وهي **لم تعمل قطّ** لأنّها كانت خلفها.

### القاعدة التي خرجت من هذا اليوم

**حارسٌ لا يعمل لا يحرس — ويتقادم في صمت.** ثلاثةُ حرّاس انكشفت متتاليةً، كلٌّ منها
كان مستوراً خلف الذي قبله، وكلٌّ منها متقادمٌ بقدر المدّة التي لم يعمل فيها:

| الحارس | ما قاله حين عمل | الحكم |
|---|---|---|
| `chunk-baseline diff` | ٤ انحدارات | **قياسٌ كاذب**: خطّ أساسٍ من ٢٠٢٦-٠٧-٢٤ يعرف ٢٫٦٪ من بنية اليوم |
| `check-min-chunk-size` | ٣٩٢ مخالفة والميزانية ٤٠ | **رقمٌ متقادم**: ٥٣٩ من ٥٤٣ حزمة مجهرية مولَّدة من `import()` لا من `manualChunks`، وأوّل فتحة تجلب ٤ حزم فقط |
| `guard:first-open-shared-tax` | `FAIL` بوقائع مسمّاة | **عطلٌ حقيقيّ في البنية** — أدناه |

### الضريبة المشتركة — أوّل نتيجة، وهي قرارُ مالك

`forumApiService` (٢١ ك.ب) و`CommunityScreen` (٧٫٦ ك.ب) **يستوردان استيراداً ساكناً**:

```
execution-dashboard-claim-pipeline      ١٦١٫١ ك.ب
execution-dashboard-persist-pipeline    ١٥٥٫٦
execution-dashboard-workspace-pipeline   ٧١٫٢
execution-dashboard-boot-pipeline        ٥٦٫٧   ← المجموع ٤٤٤٫٧ ك.ب
vendor-supabase                         ١٩٦٫٨   (CommunityScreen)
vendor-motion                           ١٣٠٫٦   (CommunityScreen)
lawyer-boot-stores                       ٢٥٫٦   (forumApiService)
```

**والرقم الذي يسمّيه الحارس ليس الرقم الحقيقيّ.** قِيس الإغلاق الساكن بنفسه (بدالّة
الحارس نفسها، متعدّيةً حتى الإغلاق):

| المدخل | حجمه | حزمُ الإغلاق | الحجم |
|---|---|---|---|
| **الإقلاع** (من `index.html`) | ٣ مداخل | **١١** | **٢٧٩ ك.ب** ✅ |
| `CommunityScreen` | ٧٫٦ ك.ب | **١٢٠** | **٢٫٤٧ م.ب** = ٢٢٪ من الحزمة كلّها |
| `forumApiService` | ٢١ ك.ب | **٨٦** | **٢٫٢٤ م.ب** |

**فالإقلاع سليم، والكلفة في فتح القسم.** وخطوط التنفيذ الأربع (٤٤٤٫٧ ك.ب) **١٨٪ فقط**
من الـ٢٫٤٧ م.ب، و**٤ من ٨٥** استيراداً ساكناً. والباقي ليس تنفيذاً: `vendor-supabase`
١٩٦٫٨ · `vendor-react` ١٣٩٫٩ · `vendor-motion` ١٣٠٫٦ · `executor-seizure-decision-queue`
١٢٠٫٤ · `lawyer-home-paint` ١١٦٫٤ · `boot-runtime` ١١٠٫٧ · ثمّ مئةُ حزمةٍ أخرى.

> **وتصحيحٌ يُسجَّل لأنّه وقع في هذه الجلسة:** قيل أوّلاً «نحو ٨٠٠ ك.ب» نقلاً عن الأربع
> التي يسمّيها الحارس، **والصحيح ٢٫٤٧ م.ب** بالقياس المتعدّي. والخطأ من العائلة نفسها
> التي شُغلت بها الجلسة: **أُخذ ما سمّاه المِجَسّ بدل أن يُقاس المقدار.**

**فلا تُكسَر الأربع لتخضير الحارس:** ذلك يُخضّره ويُبقي مليونين — وهو «تخضيرُ حارسٍ بلا
فهم سبب حمرته». والقرار كما في §٣ (قبولٌ رسميّ أم إصلاح) **يُعاد طرحُه على معطىً صحيح**،
والمقياس نفسه هو ما يجب أن يتغيّر: **حجمُ الإغلاق الساكن لكلّ مدخل قسم**، مِسنَنةً
تُقفل عند المُقاس — بدل قائمة ممنوعاتٍ بالاسم تُخضَّر بكسر أربعة أسطر.

### ما التُزم في هذه الجلسة (١٤ التزاماً، كلٌّ ببوّابةٍ خضراء على شجرته)

`e1c4deb9` قياس المتصفّح · `63765c61` الخمسة الأمنية · `4f6c09fc` حارس الفشل المُغلَق ·
`f0a7f3b2` §٠أ · `cc8c94cc`+`a8910c58` الميثاق (حقائق ثمّ سياسة) · `8fca390c` «تُجمِّد» ·
`5a5767ad` أسنان `gate-harness-paths` · `1a9b2ab6` عائلة الساعة · `ef4131c0` ملفّ مُهمَل ·
`85242594` تقادمُ توكيد · `79363cde` أقنعة ميّتة · `37d50933` عزلُ ذاكرة وحدة ·
`483c5407` تعليقٌ يُقرأ كشفرة · `c51649d1`+`b5f90be9` خطّا أساس بإذنٍ حرفيّ.

**مِسنَنة الاختبارات: ٢١ ← ٧** بلا `--save`.

### عائلةٌ واحدة فسّرت أربعة أعطال: مِجَسٌّ يُبلّغ عن نفسه لا عن المنتج

قناعٌ ميّت (`vi.mock` بمسارٍ لا يُحلّ) · ذاكرةُ وحدةٍ لا يمسّها `localStorage.clear` ·
ملفٌّ مُهمَل في `.gitignore` يُقرأ بـ`read()` · تعليقٌ يُقرأ كشفرة. **ولا واحدٌ منها
كان انحداراً في المنتج.** والدرس: قبل اتّهام المنتج، اسأل: أهو المِجَسّ؟

### فخاخُ أدواتٍ تُضاف إلى §٤

- **تعليقات CI مقصوصة عند عشرة.** تُسمّي ولا تَعُدّ — أضلّتني مرّتين. قابِلها بخطّ
  الأساس أو بقياسٍ محلّي.
- **`failures` في `test-ratchet-baseline.json` نصوصٌ لا كائنات.** `f.key` يُعطي
  `undefined` لكلّ مُدخل — نتيجةٌ صفريةٌ كاملة تعني أداةً معطوبة لا اكتشافاً.
- **`cancelled` ليست `failure`.** دفعٌ جديد يُلغي التشغيلة الجارية — ألغيتُ قياساً
  كاملاً مرّة. **لا تدفع فوق تشغيلةٍ جارية.**
- **`TZ=UTC` محلياً يُعيد إنتاج ساعة العدّاء** بلا صندوق Linux.
- **`Set-Content -Encoding utf8` يكتب BOM** — يكسر رسالة الالتزام. اكتب بـ.NET بلا BOM.
- وقائمة §١٠ في تقرير المراجعة على حالها عدا ما أُغلق أعلاه.

---

## ١ — أين نحن، بأمرٍ واحد

```bash
git log --oneline -1 && git status --porcelain && git rev-list --count hami/main..HEAD
```

**الفرع مدفوع.** البعيد `origin` = `hami` = `github.com/onebest993-wq/hami0.5` —
مستودع المالك، **وهو عموميّ** (`main` فيه يحمل ٥٠٧ ملفّ `.audit/` و٧٬٨٩٨ ملفّ مصدر).
`main` **لم يُمسّ**.

> **خطأ يُسجَّل لأنّه يخصّ ملفّ المالك:** `.claude/launch.json` كُتب فوقه بلا قراءةٍ
> سابقة أثناء إعداد معاينة المقرّ، ولا نسخة منه في git لأنّ `.claude/` غير متتبَّع.
> المكتوب الآن يعرّف ثلاثة خوادم (`dev` ٨٠٨٠ · `hq-preview` ٨٠٨١ · `e2e-preview` ٨٠٩٠)،
> فإن كان فيه غيرها فقد ذهب — ويُعاد بيد المالك.

**الشجرة نظيفة** عدا ملفّات المالك: `.claude/` · `android/npm run capbuildandroid.txt` ·
`.audit/HANDOFF-2026-09-10.md` · `.audit/CAPACITOR_BOOT_DEVICE_CHECKLIST.md` (هذا
**مولَّد** بـ`scripts/capacitor-boot-device-checklist.mjs`، فلا يُتتبَّع) · وطابع البناء
و`perf-reports/*`.

---

## ٢ — ابدأ من هنا، بهذا الترتيب

| # | الملفّ | لماذا |
|---|---|---|
| ١ | [`FOUNDATION_MAP_2026-09-12.md`](FOUNDATION_MAP_2026-09-12.md) | حالة الأساس بطبقاته: أيّها مستقرّ وأيّها رخو |
| ٢ | [`REVIEW_REPORT_2026-09-11.md`](REVIEW_REPORT_2026-09-11.md) | مراجعة مستقلّة + الخطّة مقسومةً بمالك القرار |
| ٣ | [`E2E_TRIAGE_2026-09-12.md`](E2E_TRIAGE_2026-09-12.md) | الـ٧٤ ساقطاً مفروزةً إلى جذور |
| ٤ | [`FINDING_FIRST_OPEN_MIGRATION_TIMEOUTS.md`](FINDING_FIRST_OPEN_MIGRATION_TIMEOUTS.md) | الانتظار الدائريّ تحت الأساس |
| ٥ | [`FINDING_SECURITY_SUITE_CI_ONLY.md`](FINDING_SECURITY_SUITE_CI_ONLY.md) | خمسة اختبارات أمنية حمراء على CI وحدها |
| ٦ | [`FINDING_GATES_BLIND_TO_THEIR_HARNESS.md`](FINDING_GATES_BLIND_TO_THEIR_HARNESS.md) | بوّابتان لا تعملان حين تتغيّر عُدّتهما — مُصلَح ومحروس |
| ٧ | [`FINDING_GATES_THAT_NEVER_RUN.md`](FINDING_GATES_THAT_NEVER_RUN.md) | طورُ E2E في بوّابة التنفيذ ميّتٌ منذ ٧٠ التزاماً، و١٦ بوّابةً لا يستدعيها شيء |
| ٨ | [`REVIEW_BRIEF_2026-09-12.md`](REVIEW_BRIEF_2026-09-12.md) | **إن كانت الجلسة مراجعةً مستقلّة: ابدأ من هنا** — ٢٥ التزاماً، بحجّة الخصم لكلّ قرار |

---

## ٣ — المفتوح، بمالكه

### قرارات المالك — تحجب ما بعدها

1. **ميزانية ٢٢٠ ك.ب** — هدفٌ أم حارس؟ الواقع ٥٢٧٫٧ محلياً، و٤٩٤ منها تستعملها الشاشة
   فعلاً فلا تُغلق بإعادة تقطيع. **وتصحيحٌ لمعطى هذا القرار:** كان مكتوباً أنّ «الحارس
   يعمل في CI بلا `continue-on-error`» — وهو **لم يُشغَّل في CI ولا مرّة**: خطوته (٤٦)
   كانت خلف خطوة الأمن (٣٧) التي تسقط فتتخطّى ما بعدها. رُتّبت الخطوات فصارت تعمل.
   **فلا يُبَتّ في هذا قبل قراءة أوّل نتيجةٍ لها على العدّاء** (الوثيقة ٧ §٣ب).
2. **`guard:first-open-shared-tax`** — قبولُ المقايضة رسمياً أم تكليفٌ بإصلاحها.
   **ويسري عليه ما سرى على الأوّل:** خطوته (٤٧) كانت خلف خطوة الأمن أيضاً، فلم تعمل
   قطّ على العدّاء. يُنتظر أوّل نتيجة.
3. **شكل مزامنة القضايا الجزائية** — الشريحة السادسة أُثبت أنّها **تُجمِّد** النسخة القائمة
   (صُحّح ٢٠٢٦-٠٩-١٢: كان «تُتلف» — والشفرة ترفض في `workCloudCheckpoint.ts:356` **قبل**
   `POST` في `:367`، بل و`carryForwardOmittedSlices` يحمل شريحة النسخة السابقة إلى
   الجديدة. فالكلفة رفضٌ صامت لا إتلاف — والاستنتاج يبقى، والإلحاح وحده يتغيّر).
   **وقيس على متصفّح المالك ٢٠٢٦-٠٩-١٢: `hami:criminal:case` = ٢ بايت — صفر قضية.**
   فالرفضُ بنيويّ لا عدديّ (وعاءٌ بميزانيةٍ ثابتة لمحتوىً ينمو بلا حدّ)، والفراغ
   **فرصةٌ لا نقص**: تصميمٌ اليوم بلا ترحيلٍ ولا توافقٍ رجعيّ ولا خطرٍ على محتوىً قائم.
   **أرخصُ لحظةٍ لبنائها قبل أوّل قضية.** التوصية: نقلٌ صفّاً لكلّ قضية عبر BFF.
   (§٦·٢ و§٦·٣ في `FINDING_STORAGE_CLOUD_COVERAGE.md`.)
4. **سطر `dead-modules-baseline`** الذي يُعيد مكسب `dist-hq` (−٩١٪): يحتاج
   `baseline(scope):` + موافقة حرفية (§٤).
5. **`test(` و`ci:`** — مستعملتان ١١ و٢ مرّة وليستا في الستّ (§٢·١). الحارس يفرض الستّ
   ويبدأ من `d70eacaa`؛ ضمُّهما سطرٌ في الميثاق وسطرٌ في `ALLOWED_PREFIXES`.
6. **خطّ الأساس الأوّلي لمِسنَنةٍ جديدة** — §٤ لا يذكر الحالة (F4 في التقرير).
7. **ستّ عشرة بوّابة إنتاجٍ لم يكن يستدعيها شيء** — ١٨ ملفّ `*-production-gate.mjs`
   و٣١ أمر `gate:*`، وسيرُ العمل كان يشغّل **ثلاثة**. **نُفِّذ الخيار الأوّل بإذنك**:
   `section-gates.yml` يشغّلها في مصفوفة **بلا `push` ولا `pull_request`** فلا يحجب
   التزاماً، ويدويّاً وأسبوعياً. **وساكنٌ حتى الدمج** — الجدولُ وزرُّ التشغيل كلاهما
   لا يُتاح إلا من الفرع الافتراضي. **والقرار الباقي لك:** أهذه الصيغة أم إحدى
   البديلتين في الوثيقة ٧ (وصلٌ انتقائيّ · أو حذفُ ما لا يُقصد تشغيله).
8. **عدد الحرّاس في §٣** — الميثاق يقول ٢٨، والواقع الآن **٣١** في `gate:wave0`
   و**٣٦** مربوطاً باثنين مستثنيَين. آخر زيادة `guard:gate-harness-paths`
   (انظر الوثيقة ٦): أُضيف باجتهادٍ ضمن الإذن، وهو **إضافيٌّ ويُرفع بالتزامٍ واحد** إن
   رآه المالك زائداً. وتعديل الميثاق قرارُه وحده.

### عملٌ مفتوح ومُسمّى

| البند | الحالة |
|---|---|
| ~~**ما يسقط على Linux وحده** — ثلاثة أعضاء~~ **بقي عضوان** | (١) ~~الخمسة الأمنية~~ **مُصلَحةٌ ومحروسة** في `63765c61`+`4f6c09fc` — ولم تكن عن Linux أصلاً (§٠أ) · (٢) `execution-other-party-tab:38` · (٣) `guard:tests` نفسها: تسقط على العدّاء وتمرّ محلياً ٣١/٣١. **الخطوة التالية للباقيَين: تشغيلٌ على Linux** — وقد صارت أرخص: مُبلِّغ `github-actions` مُضافٌ الآن إلى مِسنَنة الاختبارات أيضاً، فأوّل تشغيلةٍ قادمة تُسمّي الساقط بالاسم بلا اعتماد |
| `transactions-hub` (١٧) | الجذر مقيس: البذرة تحت `dev-user-uuid-1` والمركز يقرأ تحت `guest-lawyer-1`. **لم يُبَتّ**: أعطلُ مثبّتٍ أم عطلُ منتج؟ |
| الحلقة الدائرية (تخزين ↔ تعمية) | الإصلاح **قطعُ الحلقة** لا تقصير المهلة. لا يُشحن بلا إذن — وحدتان أخطرُ ما في المستودع |
| `forum-attachment-persistence` | منشورٌ يُعرض ولا يُحفظ. وله وضعُ فشلٍ ثانٍ («خارج إطار العرض») يمنع بلوغ فحص الحفظ |
| Execution Gate | **الجذر مقيس ومُصلَح**: `ReferenceError: CANONICAL_SPAWN_OPTS` كان يقتل طور E2E قبل أن يبدأ، منذ `062a25c2` (٧٠ التزاماً). الطور يعمل الآن — ونتيجته هي السؤال التالي، لا وجوده (الوثيقة ٧) |
| Lawsuits Gate | **قُرئت بالأسماء**: ٤ ساقطاتٍ كلّها مُقيَّدةٌ سلفاً في الـ٧٤ (`civil-lawsuit-scenarios` ١٩٥·٢٠٦·٢٥٨ و`civil-lawsuit-new-case` ٦٩) + متذبذبتان في `civil-lawsuit-lifecycle` (٢٧·٥١) غائبتان عن الـ٧٤ لأنّهما تنجحان محلياً. لا عطل جديد — لكنّها صارت تقول ما بها |
| ١٩ من الـ٧٤ | **شُغِّلت على خادم التطوير — والفرز كان نصفَ صحيح.** ٧ بيئيّةٌ فعلاً (`auth-onboarding-assault` · `civil-lawsuit-cloud-sync` · أغلب `wife-live-server`) و**١٢ تسقط والبيئة صحيحة**. **والجذر أُغلق بالمكدّس**: `runBffLocalAuthMigration` تمحو JWT المخزَّن في `localStorage` عمداً (هذا غرضها حين تُفعَّل مصادقة BFF) وتتسلسل إلى خروجٍ كامل، و`ensureWebMigration` ترحّل مفتاح الرمز إلى SecureStore. **عطلُ مثبِّتٍ لا منتج** — والمثبِّت لم يُغيَّر: خياراته الثلاثة ليست متكافئة وأحدها يُغيّر ما تُثبته المواصفة. §١ب في `E2E_TRIAGE_2026-09-12.md` |

---

## ٣ب — الخطّة، بترتيبها وسببه

> اتُّفق عليها مع المالك في ٢٠٢٦-٠٩-١٢. والترتيب ليس اشتهاءً: كلّ طورٍ يُرخّص ما بعده.

| # | الطور | المُخرَج | أتحتاج آلةً قويّة؟ |
|---|---|---|---|
| ١ | **الحصاد** — ادفع، واقرأ ما تقوله `guard:tests` ثمّ الإحدى عشرة | يُبَتّ في قرارَي الميزانية و`first-open-shared-tax` على بياناتٍ حقيقية | **لا** — العمل كلّه على العدّاء |
| ٢ | **صندوق Linux** | ثلاثة عناقيد لا تُحلّ من ويندوز تُحسم في جلسة | **نعم — أعلى قيمة**: يحوّل «متعذّر» إلى «محسوم» |
| ٣ | **العزل ثمّ التوازي** — عزلُ عامل E2E أوّلاً، ثمّ مُشغِّل بوّابةٍ متوازٍ | ٢١ دقيقة → دقائق · و١٥ → ٤. **يغيّر الإيقاع**: تحقّقٌ بعد كلّ تغيير | **نعم — أكبر مكسبٍ مركَّب** |
| ٤ | **الأساس ثمّ الغرف**: `transactions-hub` ← الـ١٢ في `wife-*` ← الحلقة الدائرية ← شكل الجزائي | مراجعةٌ **صغيرة** تُغلق كلّ قسم، لا كاملة | في الاختبارات لا في الكتابة |
| ٥ | **الدمج في `main`** | ليس تنظيماً: `section-gates.yml` لا يعمل أصلاً قبله | — |

**ولماذا الحصاد قبل المراجعة:** كلّ عطلٍ بنيويّ ظهر ليلة ٢٠٢٦-٠٩-١٢ كشفه **مُشغِّل لا
قارئ**. المراجعة تُجيد الحكم على العمل ولا تُنتج الوقائع — ومراجعةٌ تسبق الحصاد تحكم
على صورةٍ توشك أن تتغيّر.

**وقاعدة الآلة:** لا تجعل القرارات أصوب، **تجعل القياس رخيصاً**. والأسرع ليس الأصدق:
التوازي بلا عزلٍ يُنتج تذبذباً، والتذبذب هو الضجيج الذي يُخفي الأعطال. **العزل أوّلاً.**

---

## ٤ — فخاخ هذا الجهاز — كلّها كلّفت وقتاً مرّةً

- **أداة `Bash` تُرجع فراغاً دائماً.** استعمل PowerShell.
- **`Out-File -Encoding utf8` يكتب BOM** فيكسر كلّ قارئ JSON/git. كلّ ملفٍّ تقرؤه أداةٌ
  أخرى يُكتب بأداة `Write`. (وقع ثلاث مرّات.)
- **PowerShell: `-match`/`-notmatch` على مصفوفة** تُرجع مصفوفةً مُرشَّحة لا قيمة منطقية.
  `$body -notmatch 'x'` داخل `if` **يصدق دائماً**. استعمل `-join "\n"` أوّلاً.
- **`$rows += ...` داخل دالّة** لا يمسّ المتغيّر الخارجي. للتحليل: استعمل Node لا PowerShell.
- **قبل أيّ E2E**: `npm run build:e2e`. و`globalSetup` يرفض `dist` لا يمثّل الشجرة
  (البصمة على **محتوى** مسارات البناء، فالتزامٌ وثائقيّ لا يُبطلها). للمقارنة المقصودة:
  `E2E_ALLOW_STALE_DIST=1`.
- **المعاينة لا تُقدّم `/api/*`** — فمواصفات `wife-*` و`auth-onboarding-assault` تسقط بيئياً.
- **سجلّات GitHub Actions تتطلّب اعتماداً** حتى على مستودعٍ عموميّ، ولا `gh` هنا ولا
  توزيعة WSL. **اقرأ الفشل من التعليقات**:
  `https://api.github.com/repos/onebest993-wq/hami0.5/check-runs/{job_id}/annotations`
- **وواجهة GitHub بلا اعتماد محدودةٌ بستّين نداءً في الساعة** — لكلّ عنوان IP. استطلاعٌ
  كلّ ٤٥ ثانية يستهلكها في نصف ساعة، فيعود كلّ نداءٍ بـ`API rate limit exceeded` ولا
  تُقرأ تعليقةٌ واحدة. **استطلع كلّ ٥ دقائق لا أقلّ**، و`/rate_limit` **لا يُحسب** فهو
  الطريق لمعرفة وقت العودة:
  `[DateTimeOffset]::FromUnixTimeSeconds((irm https://api.github.com/rate_limit -Headers @{'User-Agent'='x'}).rate.reset).LocalDateTime`
- **وبناءٌ واحد يُولّد «حزمة قوانين جديدة» بلا أن يتغيّر حرفٌ فيها.** تشغيل بوّابةٍ
  تبني أعاد توليد `public/static-law-data/v1/execution.*.json` ببصمةٍ مختلفة ومانيفستٍ
  مُعدَّل. والمقارنة: **١٣٠ مادّة في الاثنين، صفر اختلافٍ في المحتوى**. والسبب مقيسٌ
  بالبايت: المتتبَّع فيه **٥٢٦ محرف `CR`** والمولَّد **صفر** — أي CRLF مقابل LF، وهو
  فرقُ الـ٥٢٦ بايت كلّه. فيختلف الـsha256، فيُكتب ملفٌّ باسمٍ جديد ويتيتّم القديم —
  ولهذا في المستودع **ثلاث نسخ** من حزمة التنفيذ. **استُعيدت الشجرة ولم يُلتزم شيء.**
  والإصلاح (`.gitattributes` أو تجزئةُ المحتوى المُحلَّل بدل البايتات) يمسّ المستودع
  كلّه بإعادة تسوية نهايات الأسطر — **فتُرك عمداً**، ويُقرَّر بعين المالك لا في ليلة.
- **وقبل أيّ E2E محلّي: تأكّد أنّ المنفذ ٨٠٩٠ حرّ.** خادمُ معاينةٍ من جلسةٍ سابقة يُسقط
  التشغيل بـ`is already used`، وهو عطلٌ بيئيّ يسهل أن يُكتب عطلاً بنيوياً.
  `Get-NetTCPConnection -LocalPort 8090 -State Listen` ثمّ `Win32_Process` لسطر الأمر.
- **GitHub يُشغّل على رأس الدفعة لا على كلّ التزامٍ فيها.** فدفعُ خمسة التزاماتٍ معاً
  يعني أنّ أربعةً منها لم تُقَس قطّ — حتى بسير عملٍ بلا مرشّح مسارات. ولذلك «لا تشغيل
  لهذا الالتزام» **لا يدلّ** على أنّ المرشّح أسقطه. (أضلّني هذا الليلة قبل أن يصحّحه
  استعلامُ الواجهة.)

---

## ٥ — أوامر تُعاد بها كلّ نتيجة

```bash
node scripts/run-gate-wave0.mjs
```

```bash
npm run build && npm run guard:lawyer-inner-weight-ratchet && npm run guard:boot-critical-weight
```

```bash
npm run build:e2e && npx playwright test e2e/home-main-interface.spec.ts e2e/lawyer-profile-z-forum-visitor.spec.ts --project=chromium --workers=1
```

```bash
npm run test:security
```

---

## ٦ — الدرس الذي تكرّر أربع مرّات في ليلة، فاكتبه على يدك

**بُنيت أربع فرضيّات وأسقطها القياس، وكلّها كانت مقنعة:**

1. مالك `inert` هو أثرُ `useLawyerDashboardMainViewChrome` — بل الأب، والأثر لم يلمسه قطّ.
2. مُفكِّك المنتدى هو بلاطة الرئيسية — بل `CommunityScreenContent`؛ إطارُ المكدّس يُسمّي
   **الدالّة** لا مَن استدعاها، والمقطع لا يُسمّي شيئاً.
3. تقصيرُ مهلة التعمية إصلاح — بل يُبدّل سلوكاً، وأسقط مواصفة.
4. عدّاء CI يصل إلى `example.supabase.co` — بل **NXDOMAIN** من كلّ مكان.

**القاعدة:** التشابه ليس دليلاً، والمقارنة المباشرة أرخص من التخمين. وكلّ مرّةٍ كانت
الأداة — اختبارٌ أو استعلام — هي التي أسقطت الفرضية، لا التفكير فيها أكثر.

---

*ولا رقم في أيٍّ من هذه الوثائق قيس على جهازٍ حقيقي. كلّه سطحُ مكتب وCI.*
