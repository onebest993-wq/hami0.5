# حالة الأساس — ليلة ٢٠٢٦-٠٩-١١

> **لماذا هذا الملفّ:** رسائل الالتزام على هذا الفرع تُحيل إلى `FINDING-024/025/026`
> و`REVIEW-03`، **ولا وجود لأيٍّ منها على القرص** (بُحث في `.audit/` كاملاً: ٢٨٠ وثيقة،
> وليست فيها). فكلّ تحليلٍ لم يُكتب هنا يعيش في رسالة التزامٍ أو محادثةٍ انقضت.
> ما يلي مقيس في جلسة واحدة، وكلّ رقمٍ فيه معه أمرُ إعادة إنتاجه.

---

## ١ — ما أُغلق، ومعه ما يحرسه

| الالتزام | العطل | الحارس الذي يخفق إن ارتدّ |
|---|---|---|
| `e49cf4fe` | `frame-ancestors` داخل `<meta>` يتجاهله المتصفّح ويسجّل خطأً | `execution-console-hygiene` |
| `85e565a9` | كتم `pointer-events` على `<html>` لا يُستردّ | اختبار وحدة + `guard:root-pointer-events` |
| `97797124` | لا حارس يمنع تكرار الكتم | **الحارس ٢٩** (بوّابة + CI) |
| `0aee38ef` | مساعد E2E يعود بالهيكل المؤقّت لا بالمحضر الحيّ | `execution-followup-tabs` |
| `899e6201` | `getAttribute` بلا مهلة و`catch` لا يُبلَغ ⇒ تعليق لانهائي | `decisions-storage-persist` |
| `a89e9ecb` | رفض دوام التخزين مسجَّل محلياً وغير مُبلَّغ | اختباران في `persistentStorageGrant` |

> `039b7d72` (تسريب `dist-hq`) **التُزم ثم تُراجع عنه في `a18a1e22`** — لا لعيبٍ فيه،
> بل لأنّه يحتاج سطراً في خطّ أساس الوحدات الميتة، وذلك يشترط إذن المالك الحرفي (§٤).
> القياس أدناه صحيح ومُعاد إنتاجه، والتغيير جاهزٌ لإعادة تطبيقه بكلمةٍ واحدة.

---

## ٢ — أكبر رقم في الليلة — **مُقاس وجاهز، ومحجوز على إذنٍ واحد**

```
dist-hq   قبل: 1136 حزمة · 11,045.6 ك.ب
          بعد:   57 حزمة ·  1,032.2 ك.ب
          −1079 حزمة · −9.8 م.ب  (≈ ٩١٪)
```

**الباب كان واحداً لا تسعة.** `src/app/utils/liveAuthUserId.ts` — مرآة هوية الجلسة،
أداةٌ مشتركة بحقّ بين المنتجين — تستورد بـ`import()` تصفيرَ قشرة المحامي عند تبديل
الحساب، فيُدرج rollup تلك الوحدة **ورسمها المتعدّي** في حزمة الإدارة:

```
hq → boot-runtime → resetLawyerSessionUiForIdentityChange
   → communityShellOpenFlow → forumIntentWarm → كل مُحمِّلات الأقسام
```

ومن هذا الباب كان **مخزنا إضابير الموكّلين** (`executionFilesStorage` ·
`lawsuitFilesStorage`) يُشحنان في بناءٍ إداري.

**والتحقّق لم يكن بالحارس وحده:** حُمّل المقرّ المبنيّ في متصفّح — ٦٤ طلباً كلّها
`200`، صفر رسالة وحدة تحكّم، والوحدات المُحمَّلة كلّها إدارية (`HqBootRoot` ·
`HqRuntimeShell` · `adminHqIdentity` · `authBoot`)، ولا `404` لشيءٍ استُبعد.

```bash
npx vite build --mode hq && node scripts/guard-dist-hq-runtime.mjs
```

### ما يحتاجه لإعادة التطبيق — وفخٌّ يستحقّ التسجيل

البديل الجديد `src/hq/stubs/hqOmitLawyerSessionUiReset.ts` **وحدةٌ ميتة** بتعريف
`guard:dead-modules`: لا يُبلَغ إلا عبر اسمٍ مستعار في البناء، ورسمُ الاستيراد الساكن
لا يتبع الأسماء المستعارة. وهذه ليست شذوذاً — **٢٢ بديلاً من بدائل المقرّ في خطّ
الأساس أصلاً** للسبب نفسه. فالمطلوب سطرٌ واحد يُضاف إلى
`.audit/dead-modules-baseline.json`، وهو `--save` يشترط §٤ لأجله إذن المالك الحرفي.

**والفخّ:** شُغّلت البوّابة قبل الالتزام فمرّت ٢٩/٢٩ — لأنّ الملفّ كان **غير مُتتبَّع**
وقتها فلم يدخل مسح الحارس. وبعد الالتزام صار مُتتبَّعاً فصارت الشجرة حمراء. أي أنّ
بوّابةً خضراء قبل التزامٍ يُضيف ملفاً جديداً **لا تكفي**؛ تُعاد بعده.

> **تصحيحٌ لسجلٍّ سابق:** رسالة `fd63f11a` تقول «ثلاثٌ منها عبر استيراد ساكن».
> القياس اليوم: **لا مسار ساكن من نقطة الدخول لأيٍّ من التسعة** — كلّها ديناميّة،
> فلا شيء منها يُحمَّل عند الإقلاع، لكنّها كانت تُشحن. البناء تغيّر منذ ذلك القياس.

---

## ٣ — عنقود التنفيذ

```bash
node scripts/run-execution-e2e.mjs
```

| | صباحاً | بعد الإصلاحات |
|---|---|---|
| سطح المكتب (`chromium`) | ٦ مواصفات حمراء | **٢٩ · ٢٩ · ٣٠ ناجحة** من ٣١ في ثلاث تشغيلات |
| زمن العنقود | ٦٫٣ دقيقة | **٢٫٨ دقيقة** |
| الهاتف (`mobile-chrome`) | **لم يُقَس قطّ** | ٢٥–٢٦ / ٣١ |

الأرقام الثلاث مكتوبة كما خرجت، لا أحسنُها: التشغيلتان الأوليان أسقطت كلٌّ منهما
مواصفةً مختلفة من مواصفتَي محضر المتابعة، والثالثة أمرّتهما معاً. الثابت الوحيد في
الثلاث هو `decisions-storage-persist`.

### المفتوح — بجذرٍ مُسمّى ولم يُصلَح

- **مركز القرارات:** قِيس أخيراً، والنتيجة **تُبرّئ المنتج**. الزرّ `disabled:false`
  و`pointer-events:auto` وغير مقفول، و**نقرةٌ حقيقية من Playwright تفتح المركز**
  (`sawDecisionsHeading: true`) — بينما `clickNativeElement`، المساعد المُركَّب الذي
  تستعمله المواصفة، لا يفتحه. ولا تظهر رسالة «لم يصل إلى الواجهة بعد» التي يعرضها
  التطبيق حين يكون الربط مفقوداً، فالربط قائم.
  **ولماذا يخفق المُركَّب لهذا الزرّ تحديداً — غير مُسمّى.**

> **وهذا يوحّد الجذرين:** إخفاقات الهاتف الأربعة و`decisions-storage-persist` تشترك
> في العَرَض نفسه — **لمسة/نقرة حقيقية تعمل، والمُركَّبة لا تعمل** — بينما المُركَّبة
> **تنجح** في فتح الإضبارة من الأرشيف على المنصّتين. فالمرجَّح سببٌ واحد في أداة
> الاختبار لا عطلان في المنتج. ولم يُثبَت بعد.
>
> **وتصحيح:** سُجّل في نسخةٍ أولى من هذا الملفّ أنّ «مركز القرارات لا يُفتح» كعطل
> منتج. القياس أسقط ذلك، والسطر أعلاه هو الصحيح.
- **تذبذبٌ يتنقّل** بين `execution-other-party-tab` و`execution-notes-persist`:
  تسقط إحداهما في كل تشغيلة والأخرى تمرّ. الشكل واحد — قشرة خارجية موجودة ومحتوى
  داخليّ حيّ غائب.
- **الهاتف:** أربع من مواصفات محضر المتابعة تسقط مجتمعةً، بينما **لمسةٌ حقيقية تفتح
  المحضر بستّة تبويبات حيّة** في مسبارٍ منفرد، والنقر المُركَّب لا يفتحه — مع أنّ
  المُركَّب **ينجح** على الهاتف في فتح الإضبارة من الأرشيف. الجذر **غير مُسمّى**.

---

## ٤ — وزن حزمة المحامي: مقايضة لا عطل

```bash
npx vite build && node scripts/guard-lawyer-inner-weight.mjs
```

```
[lawyer-inner-weight] static closure: 154 chunks, 853.0 KB gzip (budget 220 KB)
```

الأثقل: `execution-dashboard-persist-pipeline` ٤٢٫٥ · `claim-pipeline` ٤٠٫٠ ·
`executor-seizure-decision-queue` ٣٣٫٢ · `lawsuit-archive-grid` ٢١٫٤ — أي **١١٥ ك.ب
من خطوط أنابيب التنفيذ قبل أن يفتح المحامي التنفيذ**.

**وليس فوضى:** حزمة القشرة تحوي وحدتَي مصدر فقط، وتعليق
[LawyerDashboardInner.tsx:12](../src/app/components/lawyer/dashboard/LawyerDashboardInner.tsx)
يقول صراحةً: «حدّ lazy واحد من الجذع: Inner يسحب FullBoot+MainView+بلاطات المنزل
ساكناً». فالـ٨٥٣ ك.ب **قرار تصميم موثّق**، والمقايضة: وزنُ أوّل فتحة مقابل ثبات
الرسم الذي بُنيت له «الهياكل الفورية» كلّها.

**القرار للمالك**، وله سجلّ سابق في `FINDING_FIRST_OPEN_SHARED_TAX.md` و
`PHASE_CODE_SPLIT_FIRST_OPEN_CLOSURE.md` و`PHASE_PRODUCT_SIZE_FIRST_FRAME_CLOSURE.md`
— يُقرأ قبل أيّ مساس.

---

## ٥ — الأرض: ما لم يُحسم

**(أ) الفرع لم يره CI قطّ.**

```bash
git branch -r --contains HEAD
```

يخرج فارغاً. البوّابة ٢٩/٢٩ في خمس تشغيلات، وكلّها شهادة **جهازٍ واحد**.
الدفع قرار المالك وحده.

**(ب) دوام التخزين حين يُرفض.** الطلب قائم من
[bootReveal.ts:151](../src/app/bootstrap/bootReveal.ts) وله اختبار وحدة، والرفض صار
**مُبلَّغاً** بعد `a89e9ecb`. لكن ماذا **يرى المحامي**، وهل يُمنع اعتبار «حُفظ»
نهائياً قبل تأكيد السحابة — **قرار منتج للمالك**. التوصية: يُمنع.

**(ج) الشكل.** `3511` ملفّاً في `components/lawyer` مقابل `116` في
`domain + application + infrastructure`. لم يتغيّر.

**(د) سطرٌ في خطّ أساس الوحدات الميتة** يُعيد ربح الـ٩١٪ في حزمة المقرّ — §٢ أعلاه.
التغيير مكتوبٌ ومُقاس ومُتراجَعٌ عنه في `a18a1e22`، ولا ينقصه إلا كلمتك.

---

## ٦ — خمس فرضيات سقطت أمام القياس

تُكتب كي لا تُعاد:

1. **`preloadableLazy` يُعيد التركيب** بتبديل النوع — باطلة: في المسار البارد يُعلَّق
   `<Lazy>` قبل أن يلتزم، و`Resolved` يُضبط قبل استئناف React.
2. **تعليق كسول متداخل يهدم حدّ Suspense** — باطلة: النقر بعد ظهور التبويبات الحقيقية
   لا يُحدث أيّ فصل (`modalDetached=false`).
3. **السطح يرتدّ إلى الأرشيف** — باطلة: تسع ثوانٍ بلا أيّ انتقال (`d=1 archive=1`).
4. **عطل هاتفٍ في المنتج** — باطلة: لمسة حقيقية تفتح المحضر؛ الخلل في أداة الاختبار.
5. **استيراد نسبيّ يتجاوز بديل `criminalStore`** — باطلة: بصمات الحزم متطابقة
   بايتاً ببايت بعد التعديل.

> **ودرسٌ أعمّ:** مسباران من مسابيري أعطيا نتيجةً فارغة لأنّ الخطّاف **لم يُنصَب**
> (`getOwnPropertyDescriptor(CSSStyleDeclaration.prototype,'pointerEvents')` تُرجع
> `undefined` في Chrome، و`addInitScript` يعمل قبل وجود `documentElement`). ولولا
> اختبارٌ ذاتيّ يُثبت أنّ الأداة تعمل، لصار الصمتُ دليلاً. **كل مِجَسّ يُثبِت نفسه
> قبل أن يُصدَّق.**

---

## ٧ — البوّابة

`gate:wave0` صار **٢٩ حلقة** بعد إضافة `guard:root-pointer-events`.
جدول §٣ في `CLAUDE.md` ما زال يذكر ٢٨ — **متأخّر صفّاً واحداً**، ولم يُمسّ لأنّ
التسليم ينصّ على عدم تعديل الميثاق. يحتاج إذن المالك في التزام `docs(charter):`.

```bash
node scripts/run-gate-wave0.mjs
```
