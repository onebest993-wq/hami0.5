# رفع احترافية قسم الإعدادات — 2026-08-12

## ماذا يعني «عالمي» هنا

رفع **جودة المعمارية والكود** بلا تغيير بصري. ليس إغلاقاً عالمياً للمنتج (iOS / E2E / BFF).

## ما أُنجز

| تحسين | الأثر |
|--------|--------|
| شق `businessBackup.ts` (734 سطر) إلى types / encoding / range / crypto / build / import + barrel | نفس العقد العام؛ ملفات متخصصة |
| توجيه الأقسام بـ `switch` صريح بلا `as` ولا مسار خاص للمنظر | أنواع صحيحة لـ account/data |
| إزالة prefetch وهمي من تبويبات الشِل (الأقسام sync أصلاً) | صدق المسار؛ hydrator/intent-warm كما هما |
| استخراج `AppearanceBlockPicker` + `AppearanceBlockPatternGrid` | نفس الـ DOM/classes |

## التحقق

Vitest إعدادات: **79 ملف / 241 اختبار — نجاح**

## التقييم الصادق بعد الرفع

| البُعد | درجة | ملاحظة |
|--------|------|--------|
| جودة كود | **9/10** | تقسيم النسخة الاحتياطية وتوجيه صريح |
| نظافة | **9.5/10** | prefetch الوهمي أُزيل من الشِل |
| أداء | كما كان | لا قياس فتح جديد |
| أمان | كما كان | منطق التشفير/الاستيراد نُقل دون تغيير سلوك |
| موبايل | بلا تغيير بصري | |

## ما ليس «عالمياً» بعد

1. لا إثبات iOS / Face ID ميداني.
2. `services/settings` ما زال يضم تخطيط اللوحة (homeLayout) — حد مجلد لا شاشة.
3. `useBusinessBackup` (~345 سطر) ما زال hook واحداً للمجال — مقبول.
4. `guard:dead-exports` للمستودع يفشل خارج الإعدادات.

## جاهز؟

**نعم كرفع احترافي للكود داخل القسم.**  
**لا** إذا كان المقصود منتجاً عالمياً مغلقاً بكل المنصات والـ E2E.
