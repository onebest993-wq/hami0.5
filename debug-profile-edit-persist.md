# Debug Session: profile-edit-persist

Status: [OPEN]

## Symptom
- تعديلات الملف الشخصي لا تبقى بعد الخروج أو إعادة التحميل.
- الأعراض تشمل الاسم، الصورة، وقنوات التواصل.
- المستخدم يؤكد أن المشكلة ما زالت قائمة رغم عدة محاولات إصلاح سابقة.

## Scope
- شاشة الملف الشخصي للمحامي.
- مسارات الدخول في وضع التحرير، تعديل الحقول، الحفظ، الخروج، ثم hydration بعد reload.

## Reproduction
1. فتح الملف الشخصي.
2. الضغط على `تعديل`.
3. تعديل الاسم أو الصورة أو إضافة قناة تواصل.
4. إنهاء التحرير أو الخروج.
5. إعادة التحميل.
6. ملاحظة أن التعديلات اختفت.

## Falsifiable Hypotheses
1. مسار إنهاء التحرير لا يستدعي `saveProfile()` فعلياً في كل الحالات.
2. `draft` يحتوي التعديلات لكن `payload` النهائي لا ينقلها إلى `ProfileDB.saveProfile()`.
3. الحفظ المحلي يتم، لكن `useProfileLoader` أو طبقة hydration تعيد قراءة مصدر أقدم وتطغى عليه.
4. مسار الصورة/قنوات التواصل يفشل بصمت قبل اكتمال الحفظ النهائي.
5. حدث أو cache دافئ يعيد حقن بيانات قديمة مباشرة بعد الخروج من التحرير.

## Evidence Plan
- إضافة instrumentation إلى:
  - بدء التحرير وتحديث `draft`
  - استدعاء `saveProfile()`
  - `payload` قبل الحفظ
  - `ProfileDB.saveProfile()`
  - `useProfileLoader` بعد التحميل
- إعادة الإنتاج قبل الإصلاح وبعده.

## Notes
- ممنوع تعديل business logic قبل الحصول على evidence runtime.
