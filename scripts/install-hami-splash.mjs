/**
 * أصول شاشة الإقلاع أصبحت نصاً فقط («حامي») — لا صور.
 * هذا السكربت مُهمَل؛ يُبقى للتوافق مع سير عمل قديم.
 */
console.log('install-hami-splash: deprecated — boot uses text wordmark only (see index.html).');
process.exit(0);
