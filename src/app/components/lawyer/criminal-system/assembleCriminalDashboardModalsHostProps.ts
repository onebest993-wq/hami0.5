import type { CriminalDashboardModalsHostProps } from './criminalDashboardModalsHostProps';

/** تجميع props مضيف المودالات — نقطة واحدة للاختبار والتوسعة. */
export function assembleCriminalDashboardModalsHostProps(
    input: CriminalDashboardModalsHostProps,
): CriminalDashboardModalsHostProps {
    return input;
}
