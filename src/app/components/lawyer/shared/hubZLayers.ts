/** Shared z-layers used by dossier and modal shells across lawyer surfaces. */
export const HUB_SHELL_Z_CLASS = 'z-[220]';

/** New-case and nested full-screen flows above the hub shell. */
export const HUB_NESTED_OVERLAY_Z_CLASS = 'z-[230]';

/** Dossier root that sits above nested hub overlays. */
export const HUB_DOSSIER_Z_CLASS = 'z-[235]';

/** Modal layer inside a dossier surface. */
export const HUB_DOSSIER_MODAL_Z_CLASS = 'z-[250]';

/** Chrome/navigation above nested dossier modals. */
export const HUB_DOSSIER_CHROME_Z_CLASS = 'z-[252]';
