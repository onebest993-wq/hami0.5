/**
 * سياسة تسخين لوحة المحامي — intent-only بعد التشخيص (مرحلة 1).
 * لا تحميل تلقائي لكل الأقسام عند تسجيل الدخول؛ فقط hover / open / prime صريح.
 */
export const LAWYER_DASHBOARD_AUTO_IDLE_WARM = false;
