/** رمز الخادم عندما يلزم رمز تحقق جديد للفعل الخطير. */
export const HQ_STEP_UP_CODE = 'HQ_STEP_UP_REQUIRED';

export const HQ_STEP_UP_TTL_MS = 15 * 60 * 1000;

/** يظهر للمشغّل إذا نجح الإجراء وفشلت كتابة audit_logs. */
export const HQ_AUDIT_MISS_MESSAGE = 'تم الإجراء لكن لم يُكتب في سجل المقر.';
