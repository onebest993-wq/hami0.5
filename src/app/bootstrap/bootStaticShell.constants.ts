/** إزالة هيكل الإقلاع الثابت (HTML/CSS) بعد جاهزية اللوحة */
export const STATIC_BOOT_SHELL_ID = 'hami-static-boot';

/** تلاشي عند الإزالة — يطابق غلاف الإقلاع الأصلي (~150ms) دون قصّ حاد */
export const STATIC_BOOT_SHELL_FADE_MS = 180;
