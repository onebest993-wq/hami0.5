/**
 * سياق إعداد الحركة وحده — لا مكوّنات ولا محرّك.
 *
 * وحدته في المكتبة `createContext` واحد لا غير، وهي مفرودة في مقطعٍ مستقلّ
 * (`vendor-motion-config-context` في `vite.config.mts`)، فاستيرادها الساكن لا
 * يسحب `vendor-motion` إلى مسار أوّل طلاء. وهذا هو الاستثناء الوحيد لقاعدة
 * `overlayMotionRuntime`: بقيّة رموز الحركة تبقى كسولة كما هي.
 *
 * ولماذا ساكن أصلاً: مزوّد السياق يجب أن يكون **حاضراً منذ أوّل رسم**. فغلافٌ
 * يظهر بعد التركيب يُغيّر عمق الشجرة، وReact يُبيد ما تحته ويُعيد إنشاءه —
 * وهو ما كان يُبيد حالة التطبيق كلّها بعد نحو ١.٤ ثانية (FINDING-025).
 */
export { MotionConfigContext } from 'motion/react';
