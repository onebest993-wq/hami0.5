/**
 * Public execution types barrel — preserves @/app/types/execution.
 * Domain modules live under ./execution/ (Windows-safe: file + folder siblings).
 */
export * from './execution/executionShared';
