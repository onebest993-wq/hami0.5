/**
 * بديل المقرّ لـ`resetLawyerSessionUiForIdentityChange`.
 *
 * الأصل يُصفّر قشرة جلسة المحامي عند تبديل الحساب: انتظار تسخين الملف الشخصي
 * وحالة هوية الواجهة. والمقرّ لا يرسم أياً منهما، فلا شيء يُصفَّر.
 *
 * وسبب البديل ليس السلوك بل الوزن والحدّ: `liveAuthUserId` — مرآة هوية الجلسة
 * المشتركة بين المنتجين — تستورد الأصل بـ`import()`، فيُدرج rollup تلك الوحدة
 * ورسمها المتعدّي في `dist-hq`، ومنه تصل واجهة المحامي كاملةً إلى حزمة الإدارة.
 * استيرادٌ ديناميّ واحد في أداةٍ مشتركة كان يجرّ الرسم كلّه.
 *
 * لا شيء يُكتم صامتاً هنا: الدالّة `void` لا تُرجع قيمةً يقيسها أحد، ولا تلمس
 * تخزيناً ولا مزامنة — فتصفيرُ لا شيءٍ هو التنفيذ الصحيح لا حيلة إسكات.
 */
export function resetLawyerSessionUiForIdentityChange(): void {
    /* المقرّ لا يملك قشرة جلسة محامٍ */
}
