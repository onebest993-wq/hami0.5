/** بديل بناء المقر — جدولة نسخ إضابير المحامي ليست سطح المقر. */
export async function writeProtectedBackupFromRaw(): Promise<void> {
    /* HQ product excludes lawyer protected backups */
}

export function scheduleProtectedBackupFromData(): void {
    /* HQ product excludes lawyer protected backups */
}

export function scheduleProtectedBackupFromRaw(): void {
    /* HQ product excludes lawyer protected backups */
}
