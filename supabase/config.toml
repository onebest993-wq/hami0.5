# Hosted Auth templates are in the Dashboard (not this stub).
# Copy supabase/templates/magic_link.html → Authentication → Email Templates → Magic Link
# so HQ OTP is 6 digits ({{ .Token }}) instead of a broken ConfirmationURL.
# daily-auditor retired — see migration 032_drop_abandoned_rag_auditor.sql
