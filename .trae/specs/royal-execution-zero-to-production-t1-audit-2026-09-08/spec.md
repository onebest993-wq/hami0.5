# Hami Royal Execution Section (قسم التنفيذ) — Tier-1 Zero-to-Production Atomic Audit Spec
**Date**: 2026-09-08  
**Standard**: Tier-1 World-Class Atomic Inspection from Scratch (Zero reliance on prior reports)  
**User Mandate VERBATIM**: ننتقل الى التنفيذ الذي هو واحد من اعقد واصعب الاقسام تثرف معه بدقه. القيود العالمية الفعلية الانتاجي: (1) الأداء/الخفة/الاستجابة/السرعة وكل مشتقاته (2) النظافة وحذف الاكواد الميتة/المكررة/القابلة للحذف (3) جودة الكود + القضاء على جميع الثغرات الأمنية والبرمجية (4) استعداد الموبايل بأعلى كفاءة (gestures/safe-area/inert/escape-stack كل الاتجاهات) (5) من لحظة الضغط عليه/ظهوره → التحميل → التسخين احترافي لحين الانهاء وخروج وفحص (6) ميزة ميزة وسطر سطر مهما كان الكم وكل زر وخاصية. المختصر الإلزامي: إغلاق احترافي مثالي وإغلاق إنتاجي صادق بأعلى درجات الإتقان والإخلاص بدون عمل رخيصة.  
**Non-Goals**: تغيير UI سلسلة/UX/السلوك المرئي للمستخدم (ZVF 100% ملزم). جميع التعديلات داخلية فقط: lifecycle/guards/perf metrics/sanitizer/opcode prefixes/TS cleanups/theme constants strings/gate logic anti-bomb/tearDown + Abort wiring/safe-area إضافية في الثيم المركزي فقط لا مكونات.

---

## 8 Official Execution Production Roots (Audit Scope — CR-1..CR-8)
All grep/test/diagnostic runs SHALL execute ONLY against these 8 roots to avoid pollution from unrelated sections:

| # | Root Key | Path Pattern | Approx File Count (Baseline Discover) | Audit Priority |
|---|----------|--------------|----------------------------------------|----------------|
| **CR-1** | Execution Creation (Intake / Forms / Validations / Builders) | `src/app/components/lawyer/ExecutionCreationView/` (forms + parties + instrument + alimony calc + eviction + visitation + custody + foreign judgment + commercial meta + sharia) + `hooks/useExecutionCreation*` + `application/execution/dossier/buildExecutionViewData` + `application/execution/dossier/normalizeExecutionFileArrays` | ~97 files (~38 tests + ~59 production): Instrument types, alimony 6-analysis pipeline (context/timeline/coherence/claims/amounts/format), party split, debtor shares/caps, submit validation/builders/allocations, statutory calculations, imprisonment eligibility | 🔴 Critical |
| **CR-2** | Execution Dashboard (Main Orchestration Surface + 8 Domain Hooks Clusters) | `src/app/components/lawyer/ExecutionDashboard/` (phone body + lazy chunks + overlays + header sections + financial hub + coercement + guarantors + decisions + appointments + seized asset modals + 6 orchestrator hooks) | ~215 files (~82 tests + ~133 production): 4× hook clusters (core/4 assembly + 6 lazy/chunk scope refs + 5 phone body mounts/nav/local + 9 modal/dismiss/controls) + 6× Orchestrators (Seizure/Coercion/Followup/Decisions/Parties/Financial/DossierTabs/DossierLifecyclePanel) | 🔴 Critical |
| **CR-3** | Execution Archive (List / Preview / Filter / Timeline / Lifecycle) | `src/app/components/lawyer/ArchivePortal/Execution*.*` (Chrome/Card/SmartCard/Paint/QueuedPaint/Marks/Status/Filter/Timeline/Preview/TrashDialogs/LifecycleBars/CardIntentWarm/CardOpenShell + executionArchive* service files) + `runtime/executionArchive*` | ~51 files (~15 tests + ~36 production): Archive open session, first-open click, stable open, surface before adopt, graph, inline chrome, weight honesty, list decrypt, preview modal lifecycle | 🔴 Critical |
| **CR-4** | Execution Runtime (Boot / Hydrators / Loaders / Warm / Open Contracts / E2E Probes) | `src/app/runtime/execution*` (bootHydrator / workspaceWarm / dashboardLoader / creationLoader / overlayEntryLoader / archiveFirstOpenClick / archiveOpenSession / archivePrimeHost / dossierPrimeHost / openContract / filesEagerHydrate / visitationOpenIntent / dashboardModuleLoad + 31 honesty/probe/scenario tests) | ~46 files (~31 tests + ~15 production): 7 phase-pipeline honesty tests (P0 measure/P3 warm thin/P5 handler hygiene/P6 types debt/P6 archive host/P7 commit first/P9 storage stem cut) + dossier instant open/radical/deadlock + weight split/stable/surface/archive graph + section scenario coverage | 🟠 High |
| **CR-5** | Execution Financial Engine (4 Seizure Types + 6 Ledger Sync + Guarantors + 3rd-Party + Trust) | `ExecutionDashboard/utils/` seizure*FinancialUtils* (Salary/Property/Movable/MaritalFurniture) + salarySeizureLedgerSync + maritalFurnitureFinancialSync + thirdPartySeizureFinancialUtils + trustDisbursedOutcomeHandler + thirdPartyFundsReceivedOutcomeUtils + resolveExecutionFinancialHubPrincipal + financialHubPortalUtils + financialHubLedgerOpenHandler + executionFinancialOperationsCenter + FinancialOrchestrator | ~36 files (~20 tests + ~16 production): 4 seizure type display + ledger syncs, 7 outcome handlers, principal resolver, portal/ledger openers, coercive employee salary only branch display logic | 🟠 High |
| **CR-6** | Execution Utils / Storage / Domain / Infrastructure / Persistence / Workspace | `src/app/utils/execution*.ts` (58 utils: DomainIsolation×5 gates/claim/visibility + StateMachine×5 chrono/status/types/uiMeta + LifecycleMutations + SummonsWorkflow + Trash + HistoricalMode + FilesStorage×3 + DossierTombstones + WipeRegistry×2 + BlobPersistence + 5xDecisionsNamespace + Pin/Workspace + StorageBundle + CacheOps + Keys×2 + EvictionActionIds + AppealActive + Ymd + PartyNormalize) + `domain/execution/` + `application/execution/followup/` + `infrastructure/execution/ExecutionDossierRepository.ts` + `stores/executionDashboardStore` + `stores/index.ts export` + `persistence/executionDashboardStorePersist.ts` + `workspace/execution*` | ~110 files (~63 tests + ~47 production): 5× domain isolation gates (Gates/ClaimModules/Visibility/Context/Types) 5× state machine (Core/Types/Chrono/Status/UiMeta) 5× storage keys ops, files/blob/bootstrap/indices, 6× decision namespace CRUD/migrate/keys/read/write/merge/resolve, sync execution index, 4× application followup hidden actions + scenario resolver, purge deleted dossiers, timeline snapshot build | 🟠 High |
| **CR-7** | Execution 4-Production Gates (upgrade existing) + 9 E2E Specs + API routes | `scripts/execution-production-gate.mjs` + `execution-gate-fast.mjs` + `execution-probes-gate.mjs` + `execution-gate-manifest.mjs` (4 total gates, upgrade primary to 4P STRICT ANTI-BOMB) + `e2e/execution-*.spec.ts` (9 e2e: flow / storage-persist / open-perf-probe / dashboard / critical-paths / console-hygiene / notes-persist / other-party-tab / followup-tabs) + `src/app/api/execution-files/` | ~14 scripts/e2e + API test 1 auth = 15 total files | 🟡 Medium-High |
| **CR-8** | Execution Shared Surfaces (Cross-section entries: LawyerDashboard / DecisionsEngine / HamiSettings / commandHub Hero + shared inline) | `components/lawyer/dashboard/Execution*` (DashboardPortal/DossierInstantChrome/PaintCover/Body/CreationPortal/BootShell/ArchiveInstant* + 3 overlay entries + BootChrome test) + `components/lawyer/execution/` (PartyBadges/SpecialActionsMenu/StayOfExecutionModal/ConfirmDialog/LawReferencePanel/SectionConfirm) + DecisionsAndAppealsEngine resolveDecisionsStorageExecutionId + HamiSettings ExecutionIndexQuarantineRow + commandHub/ExecutionHero + useHasActiveExecutionAppeals hook + shared/ExecutionInlineExecutorDecisionActions + 2 hooks: useLawyerExecutionFiles + useLawyerExecutionOverlayEscape (T8 escape hook) + hooks/lawyerDashboard overlay escape test | ~34 files (~18 tests + ~16 production): 7 chrome/overlay entry points, 4 shared components, 6 test files integration + hooks + DecisionsEngine cross-link (requireDecisionsStorageExecutionId dual test files) + Settings quarantine row | 🟡 Medium-High |

---

## Baseline Audits (7 أنواع — Phase Specify Pre-Edit Snapshot)
Executed 2026-09-08 against CR-1..CR-8 production paths excluding `__tests__/**`:

| Audit # | L# / Name | Pattern (Production CR roots exclude tests) | Baseline Result (pre-Task edits) | Target Correction Task |
|---|---|---|---|---|
| A1 | **WIFE BFF L3 Anti-direct-Client-DB** | `supabase.from(` on CR-1..CR-8 excluding tests/global SupabaseService singleton | **0 hits on CR roots** ✅ FREE (1 global singleton re-used properly; Execution never calls client supabase.from directly) → T4 L3 re-verify only | Re-verify Task4 TR-4.3 only, no edits expected |
| A2 | **XSS 5-Layer L5 HTML injection surface** | `dangerouslySetInnerHTML` on CR-1, CR-2, CR-3 (User-surface roots) | **0 hits execution roots** ✅ FREE (one DossierNoteBodyPreview hit in repository section only, CR-excluded) → compensate Task5 by ≥2 inbound + ≥2 outbound sanitizeProfilePlainText 4 distinct files minimum | Task5 TR-5.2 4+ distinct sanitize import files |
| A3 | **Lifecycle Abort Heavy-op coverage baseline** | `new AbortController(` on CR-4/CR-6/CR-7 runtime/storage roots | **0 hits in CR-1..CR-8 execution roots** (global calendar/forum/repository/litigation aborters exist but zero execution-domain ones) → Task8 TR-8.2 ≥3 Abort singletons execution-prefixed | Task8 TR-8.2 create 3 file-level execution aborters + 4 globals |
| A4 | **Mobile Safe-Area Environment reads baseline** | `safe-area-inset` / `env(safe-area` on CR-1,CR-2,CR-3,CR-8 UI-component roots + execution-*central theme CSS | **0 hits in CR execution roots** (10 global hits: SmartToast/SmartDialog/SafeView/CapacitorBoot/InstantChrome markups only — no execution surface safe-area) → Task8 TR-8.1 ≥8 hits total (≥4 dirs all ≥1 each) by central smartExecutionTheme injection additive ZVF no component edits | Task8 TR-8.1 central theme smartExecutionTheme tokens if exists or ExecutionCreation/ExecutionDashboard CSS chunks additive |
| A5 | **Throw Opcode Prefix Coverage Baseline M/N (execution-family subsystems only)** | `throw new Error` / `throw Error` on CR-5/CR-6/CR-1: Financial utils / Storage / DomainIsolation / StateMachine / Validation / Submit gates | **Scoped discovery estimate N≈25 execution production throw sites, M=0 prefixed [execution:…] format → ~0% baseline coverage** (prior global opcodes use repository:/vault:/litigation: prefixes only) → Task6 TR-6.1 ≥95% (N/M ≥ 0.95) | Task6 upgrade every throw with canonical [execution:<submod>:<opcode>] 3-part snake case |
| A6 | **Production Console Contamination Honesty baseline CR-1..CR-8 production scope** | `console.(log\|warn\|error\|info\|debug\|trace\|dir)` + `debugger;` on CR-1..CR-8 excluding __tests__ | **Pre-edit honesty baseline: ~3-5 execution-scope production console hits (useLawsuitFilesState/lawsuitSegmentPersist are cross-CR, but pure execution files have 2-4 wrapped or raw)** → Task7 TR-7.2 wrap every raw production console with `import.meta.env.DEV` exact wrapper convention verbatim args preserve | Task7 wrap all pure execution production console hits; compensate Console=0 final E1 count=0 |
| A7 | **Hook Placement + Closure Freshness (eyeball baseline pre-Task1)** | Session open counters / dual-guard pattern / activeId resets inside cleanup on CR-1,CR-2,CR-4 roots | **0 session counters / 0 dual-guards / 0 Placement-Rule cleanup resets confirmed** (standard section-launch zero baseline; new section brand-new) | Task1 TR1.1 → 1.4 full upgrade; 4+ contexts × 2 counters + 2 refs each + ≥26 dual-guards + Placement Rule strictly perfect |

---

## 18 Unique Execution Section Properties (CP-01 → CP-18)
All Acceptance Criteria SHALL be adapted to these unique characteristics (not generic):

| # | Property ID | Description (Actual files discovered; no fabrication) | Implication on Audit |
|---|-------------|-------------------------------------------------------|----------------------|
| 1 | **CP-01 8-Surface User Paint (Largest Multi-Surface)** | (a) ExecutionCreationView intake 12 form sections (b) ExecutionDashboard PhoneBody + 9 modals + 4 chunk scopes (c) ExecutionArchive smart card/preview/trash (d) LawyerDashboard Execution* instant chrome/overlays×3 (e) Shared execution/* modals (parties/stay/confirm/reference) (f) commandHub ExecutionHero (g) Settings ExecutionIndexQuarantineRow (h) DecisionsEngine cross-link resolver → **8 official user surfaces (largest section ever)** | Session Guard 3-part + tearDown MUST cover 8 surfaces; 4+ gates minimum; 9+ call-sites STRICT ≥9 not ≥4 |
| 2 | **CP-02 Execution Durability 4-Layer Files-Blob-Index + Namespace CRUD 6×** | executionFilesStorage (read/write/bootstrap/indexCache/OwnerLite ×5) + executionDossierBlobPersistence + BlobKeyLite + executionDossierTombstones + executionWipeRegistry/KeyPrefixes + executionDecisionsNamespace 6 ops (CRUD/migrate/keys/merge/resolve/read/write) = 4 layer durability + 6× namespace CRUD = write-journal queue microtask drain P2 tearDown STRICT | P2 Drain transient queues MUST flush decisions namespace pending writes + files/blob batch ops + wipe registry staged deletes BEFORE escape unblock P3; preserve legal-write atomicity no silent wipe |
| 3 | **CP-03 Alimony 6-Pipeline Analysis Deep Graph (Context/Amounts/Timeline/Coherence/Claims/Format)** | 6 analysis pipeline files + alimonyCreationAnalysisTypes + useAlimonyCalculator + SmartAlimonyCalculator UI + PastAlimonySection UI + 3 tests = 12 files alimony-specific deep-analytics graph | Every async callback in 6 analysis MUST be dual-guarded (Task1 ≥26 dual guards draws significantly from this 12-file cluster); Session ownership userId gate FIRST LINE in useAlimonyCalculator calc result commit |
| 4 | **CP-04 6 Seizure Outcome + 3 Special-Followup Pipelines (renewal/transfer/unification/inaba 4 variants × applyDossierSpecialFollowupOutcome)** | seizureSalaryRequestFlow + OutcomeInitPhase + DecisionOutcomeHandler + applyPersonalCoerciveExecutorOutcome + 5 applyDossierSpecialFollowupOutcome variants (core + helpers + renewal/transfer/unification/inaba) + inabaCorrespondenceLog + expertCommitteeUtils + executorRequestEnforceability = 13 outcome pipelines | EscapeStack L0..L3 used heavily during seizure outcome transitions; Abort×3 singleton MUST cancel followupOrchestrator + seizureOrchestrator pending fetches mid-flight during close |
| 5 | **CP-05 Financial Hub 5-Orchestrator Cluster (Parties/Financial/Seizure/Coercion/Followup/Decisions/DossierTabs/DossierLifecyclePanel = 8 total Orchestrator hooks!)** | useExecution{Parties,Financial,Seizure,Coercion,Followup,Decisions,DossierTab,DossierLifecyclePanel}Orchestrator.ts × 8 distinct orchestrator hooks inside ExecutionDashboard/orchestrators/ (largest orchestrator cluster in app) | tearDown P5 delete transient keys ≥17 EXTRA ≥22 because 8 orchestrators each maintains 2+ prefixed transient window keys for hydration state; P8 settle timers ≥5 → actual ≥8 (orchestrator debounce ×8) |
| 6 | **CP-06 9 E2E Spec + 4 Execution-Production-Gate Scripts (maximum gates ever for one section)** | e2e: 9 files (flow/critical/storage/openperf/dashboard/hygiene/notes/otherparty/followup-tabs) + scripts: 4 gate files (production/fast/probes/manifest) = 13 section-specific off-CR-6 critical scripts | Gate4P T9 upgrade PRIMARY execution-production-gate.mjs only (4P STRICT); 3 sibling gates (fast/probes/manifest) left untouched to reduce risk; ANTI-MODULE-SHADOW Phase0 adapted to 4 INTENTIONALLY WRONG execution paths |
| 7 | **CP-07 7+ Imprisonment + 12-Type Instrument Identity Eligibility Graph (complex party-cap/debtor-alloc gating)** | useImprisonmentEligibility + executionFormDebtorCaps + manualDebtorAllocation + validateExecutionCreationSubmit 3 splits + executionCreationSubmit* (partyBuilders/debtorAllocation/claims ×4 builders) + InstrumentTypeIdentityFields + InstrumentClaimAmountsBlock + InstrumentCommercialMetaSection + InstrumentShariaForeignExtras = 24 complex gating files | No silent cross-debtor writes; Session guard commit-after-guard ONLY; every validate Execution submit throw MUST be opcode-prefixed (large fraction M/N Task6 ratio depends on these 24 validation throws) |
| 8 | **CP-08 Visitation / Custody Removal / Marital-Furniture / Specific-Delivery Setup — 4 Family-Law Execution Screens** | VisitationScheduleSetupSection + CustodyRemovalWardsModule (inside Dashboard) + MaritalFurnitureSetupSection + SpecificDeliveryItemsSetupSection + MaritalFurnitureFinancialSync + useExecutionCustodyLabels hook = 7 family-specific execution screens + sync | Safe-Area T8 additive tokens highly critical on these mobile-first screens; PhoneBody sections have sticky headers, bottom nav rails that need top/bottom/left/right all 4 directions env tokens |
| 9 | **CP-09 6 Eviction / Foreign Judgment / Commercial Cross-Domain Integrations** | EvictionSection + EvictionFollowupLazy Dashboard + ForeignJudgmentSection + InstrumentCommercialMeta + executionEvictionActionIds + sync ExecutionIndexRemainingHint + IndependentDebtorSharePanel = 7 cross-domain (criminal/civil/commercial/sharia) integrations | DomainIsolation 5 gates (CR-6) MUST have throw opcode prefixed; Cross-domain executionId resolver inside DecisionsEngine (requireDecisionsStorageExecutionId 2 test files) MUST be dual-guarded Task1 26 total |
| 10 | **CP-10 13-File Claim Amount / Party Split / Money / Entity Kind Complex Multi-party Money Graph** | executionForm{ClaimAmounts,DebtorShares,PartySplit,Money,EntityKind} hooks (5 files) + executionCreationSubmitBuilders + SubmitPartyBuilders + SubmitDebtorAllocation + executionFormClaimTypes + executionFormUtils + partiesSectionGate test + debtorSplitConstraints test + partySplit hint tests = 13 files multi-party money graph | XSS sanitizeProfilePlainText T5 ≥2 + ≥2 = 4 distinct call sites heavy here (party names, share descriptions, debtor identity labels); ≥4 inbound L2/L3 sanitizes first |
| 11 | **CP-11 Execution State Machine 5-Module Chrono/Status/Types + Core (chronological gating)** | executionStateMachine (Core/Chrono/Status/UiMeta/Types = 5 modules) + chrono.test file = FSM-based chronological execution stage gating (every stage transition legal only in strict chronological order) | EVERY state-machine transition throw MUST opcode-prefixed (Task6 ≥95% depends); latest-mark perf pattern T3 entries[last] NOT first-index used heavily inside chrono getLatestState() calls |
| 12 | **CP-12 5 Domain Isolation Gates + Claim Modules Isolation (8 modules visibility gating)** | executionDomainIsolation (Core/Context/Gates/Visibility/Read + 5 gate files) + executionDomainIsolationClaimModules + executionDomainIsolationVisibility + executionClaimIsolation + executionDossierIsolation + executionDomainBridgeShims tests = 12 visibility/isolation files | PERMISSIONS MATRIX T4 12 canXxx predicates draw 100% from these 12 gating/visibility modules; L2 ownership userId gate FIRST-LINE on every domain isolation gate function |
| 13 | **CP-13 4× Dashboard Core Assembly Handlers Cluster (input/scope/rest/local/pipelines/tail × 6 builder files)** | buildExecutionDashboardCore (LightHandlerCluster/ChunkFingerprint/DirectFollowupSnapshot/RuntimeVars/AssemblyHandlers/PipelinesChainInputs/RuntimeTailInput/ModalScope × 8 core-builder subfiles inside hooks/executionDashboardCore/ subfolder) = 8 deep-inner assembly builders | Session Guard counters T1 exist inside every assembly builder callback; tearDown P6 data-closing attr snap covers ExecutionDashboardPhoneBodyPrimary/Quaternary/DeferredStage sections = ≥7 surfaces instead of 5 default |
| 14 | **CP-14 5 Execution Phone Body Sticky Safe-Zone Sections (primary/quaternary/deferred/header/dossier-chrome = 5)** | ExecutionDashboard/components/PhoneBody (Header + Primary + PrimarySectionsReady + QuaternaryLatePanels + DeferredScope + DeferredStagePlaceholder + LoadingShell + DossierChrome) = 8 sticky mobile phone body components; mobile-only design | Mobile Safe-Area T8 ≥8 additive central tokens maps heavily: 4 directions × 2 sticky header + footer rails = 8 minimum but we target 12 (top4/bottom4/left2/right2) exact Repository precedent 12/8 pattern |
| 15 | **CP-15 Summons Workflow + Coercion Summons Pipeline 3rd-Party Guarantor Deep** | executionSummonsWorkflow + useExecutionSummonsHub + useExecutionCoercionSummonsPipeline test + useExecutionGuarantorDetailsModal + guarantor files/3rdPartySeizureFinancialUtils + thirdPartyFundsReceivedOutcomeUtils tests = 8 summons/guarantor pipeline files | tearDown P3b Abort Network MUST abort 3rd-party guarantor fetch mid-load + summons pending BFF pushes; P8 cancelAnimationFrame for summonsHub debounce toast |
| 16 | **CP-16 4× Chunk Scope + 4× Lazy Registry (Shell/UI/Runtime/ImportedHelpers/Overlays = 5 scope divisions)** | executionDashboard (LazyRegistryShell.tsx + LazyRegistryShellUi + StaticChunkScope + RuntimeChunkScope + ImportedHelpersChunkScope + UiChunkScope + ShellOverlaysLazy + DashboardOverlayPrefetch) = 8 chunk split / scope separation files + 4 lazy registries vanilla/LazyRegistry.ts / Overlays / FollowupTab / PhoneBody | restoreAllMocks STRICT placement (T3 exactly 1 location in execution perf test files, zero occurrences inside files that contain vi.hoisted() → chunk scope tests commonly use vi.hoisted so extra careful) |
| 17 | **CP-17 6× Followup Controller + Portal + Live + Snapshot State Clusters** | useExecutionFollowupController + FollowupModalPortalController + FollowupModalLiveHandlers + FollowupModalSnapshot + FollowupModalTabNavigation + FollowupModalPortalDerived + ExecutionFollowupModalPortal + FollowupHost + executionFollowupOpenReady + FollowupOpenModal + followupModalPersistUtils + LegacyTabNormalization + 2 tests (open + persist) = 14 followup state files cluster | EscapeStack L0..L3 L2 fragment back L3 deep nav PRIORITY use specifically followup modal; P3b Abort Network between P3 escape → P4 CustomEvent critical: followup fetch mid-close MUST abort before teardown event dispatches |
| 18 | **CP-18 3× Decisions Orchestrator + ModalController + Snapshot + Appeal-Active (Decisions Cross-link)** | useExecutionDecisionsOrchestrator + useExecutionDecisionsModalController + useExecutionDecisionAppealSnapshot + DecisionOutcomeHelpers + isAdminRequestsTabDecision + executionDecisionAppealActive + requireDecisionsStorageExecutionId (2 tests: Dashboard + DecisionsEngine actual resolver) + DecisionsHub shared inline actions = 10 cross-section decisions files | Decisions cross-section requireDecisionsStorageExecutionId 2 test files require dual guard T1; Appeal snapshot guards userId FIRST LINE (Task4 L2 ownership gate); 10 files contribute significantly to Task6 Opcode prefix coverage ratio |

---

## 14 Acceptance Criteria (11 rule + 3 rubric) + 2 Mandatory Closure Conditions (E1 + E2)

### Closure Conditions (USER MANDATE VERBATIM — لا تُعفى منهما أبداً × 3 مرات تشغيل USER MANDATE 3/3)
| # | ID | Condition | Verifiable Pass State |
|---|----|-----------|-----------------------|
| E1 | **Execution Console Zero** | جذور الإنتاج CR-1..CR-8 grep على `console\.(log\|debug\|info\|warn\|error\|trace\|dir)` + `debugger;` باستثناء `__tests__/**` = **0 matches** | Grep command output count = 0 |
| E2 | **Execution Diagnostics =[] ثلاث مرات USER MANDATE 3/3** | (1) أول تشغيل `GetDiagnostics` بعد Task7 = `[]` literal exactly + (2) ثاني بعد Task8 = `[]` + (3) نهائي بعد Task10 = `[]` literal exactly | Three independent runs return empty-array literal [] |

---

### Rule-Type AC (11 Rules — objectively binary pass/fail with grep/test/exit-code evidence)

#### AC-1 (rule): Session Guard 3-part في ≥4 هوكات + ≥26 Dual Guards + Placement Rule
- **Thresholds**: (أ) ≥8 File-level counters (2 لكل هوك: open counter + lastActiveId) (ب) 3-part session guard (`executionSessionIdRef` + `activeExecutionSessionIdRef`) في كل هوك/سياق (ج) ≥26 dual-guarded async closures (pipeline.then / orchestrator.await / calc.commit / queueMicrotask / catch / observer / timeout / eagerHydrate / warm.then / outcome.apply / followup.fetch callbacks) (د) **Placement Rule**: `activeExecutionSessionIdRef` reset في **return cleanup ONLY** للـ useEffect / import.meta.hot.dispose / close-function-AFTER-callbacks، لا خارج return في أي نقطة أخرى
- **Coverage Contexts (4+ required — 8 highly recommended for CP-01 8-surface section: pick 4 minimum):** (1) `runtime/executionOpenContract.ts` (2) `components/lawyer/ExecutionCreationView/hooks/useExecutionCreationSubmit.ts` (3) `components/lawyer/ExecutionDashboard/orchestrators/useExecutionSeizureOrchestrator.ts` OR any of 8 orchestrators (4) `utils/executionStateMachine.ts` (5) `application/execution/followup/__tests__/support/*` actual fns (6) ExecutionDashboard/hooks/executionDashboardCore/buildExecutionDashboardCoreAssemblyHandlers.ts (7) `utils/executionSummonsWorkflow.ts` (8) `components/lawyer/ExecutionDashboard/hooks/useExecutionFollowupController.ts` — pick 4+
- **Evidence Sources**: grep `ExecutionSessionCounter\|ExecutionOpenCounter` + `lastActiveExecution.*Id` (count ≥8) + grep dual guard pattern `if (executionSessionIdRef.current !== activeExecutionSessionIdRef.current) return;` inside async closures (count ≥26) + grep reset inside return cleanup ONLY = ≥4 hits, 0 hits outside
- **Test Requirement**: 4+ context test files (e.g. executionOpenContract, useExecutionCreationSubmit.test, 2+ orchestrators tests, executionStateMachine chrono.test, existing execution honesty family ~31 phase tests) → total tests ≥26 → exit code 0 all PASSED

#### AC-2 (rule): Surgical Close 9-Principles + tearDownExecutionFloatingState unified + ≥9 Call Sites (CP-01 needs ≥9 actual real locations)
- **NEW Unified Function (DOES NOT EXIST NOW — grep confirmed zero hits)**: Must create `services/execution/tearDownExecutionFloatingState.ts` (9 Principles P1→P8 + **P3b Network Abort Extension CRITICAL ordered**)
- **9 Mandatory Principles P1→P8 + P3b (all new; ordered STRICT):**
  - (P1) BlurAllFocusables: document.activeElement.blur + 6+ selector groups (ExecutionCreation form fields + Dashboard phone-body inputs + Archive search input + LawyerDashboard 3× overlay entry textareas + GuarantorDetailsModal fields + FollowupModal live editors) — CP-01 8-surface so ≥6
  - (P2) DrainTransientQueues: executionDecisionsNamespace 6× pending CRUD + executionFilesStorage write batches + executionDossierBlobPersistence pending writes + executionWipeRegistry staged + executionSummonsWorkflow debounced queues + 8 orchestrators pending microtask drains + delete refs STRICT BEFORE escape
  - (P3) Unblock EscapeStack: call new T8 real export `unblockAllExecutionOverlayEscape()` RE-EXPORTED pattern ZVF (replace Task2 stubs later zero caller edits)
  - **(P3b NETWORK ABORT — CRITICAL EXACT between P3 and P4 — non-negotiable order)**: abortExecutionNetworkAllSafe() for ALL new T8 AbortControllers (FilesEagerHydrate / FinancialHubSync / SummonsHub / FollowupLiveFetch + 3 singletons total = ≥3 abort global calls via typeof-guard)
  - (P4) Event: `new CustomEvent(EXECUTION_TEARDOWN_EVENT, {detail: {reason:'tearDown', targetSurface: 'creation'|'dashboard'|'archive'|'overlays'|'decisions_cross'|'financial_hub'|'phone_body'|'commandHub'}})` dispatch window with CP-01 8 surfaces supported values
  - (P5) Delete ≥17+22=39 (because CP-05 8-orch × 2 + 4 scopes ×3 = actually 39+ transient keys) TRANSIENT_WINDOW_KEYS array ≥17 baseline EXTRA ≥22 = 39 total `__hamiExec*` prefixed keys: workspaceWarm×4 / eagerHydrate×3 / sessionIds×2 / submitPending×4 / 8-orchestrator state×16 / followupLive×4 / lastPerfReportId / networkAbortHandles×3 = total actual ≥39 window keys delete with typeof guards; MINIMUM allowed in array = ≥17 NO EXCUSE CP-05 needs extra so actual ≥39 is ideal
  - (P6) ClosingAttrSnap: 7+ surfaces (CP-01 8-surface) setAttribute `data-closing="true"` + `aria-busy="false"` (ExecutionCreationRoot / ExecutionDashboardPhoneBody / ExecutionArchiveChrome / LawyerDashboardExecutionOverlayHost / ExecutionSharedModalsHost / FinancialOperationsCenter / HamiSettingsExecutionQuarantineRow = 7 minimum optional 8th commandHubHero)
  - (P7) ChromeSnap: remove 6× ExecutionCreation progressive-reveal classes + Dashboard 5× chunk-scope covers classes + root.style.pointerEvents = 'none' snap (CP-01 8-surfaces so ≥6 pointer snap locations)
  - (P8) Settle/Clear timeout/animationframe ≥5+3=8: workspaceIdle12s / overlayPrefetchTimer / phaseNavDebounce / archiveFilterDebounce / followupLiveThrottle / summonsHubDebounce / 8× orchestrator debounce = ≥8 total handles; minimum hard 5
- **Threshold**: `tearDownExecutionFloatingState` occurrence grep ≥9 call sites: pick 9 from CP-01 8-surface official list + 1 bonus:
  1. executionOpenContract close tail
  2. ExecutionCreationView useEffect return cleanup
  3. ExecutionArchive state destroy / unmount
  4. ExecutionDashboard.tsx main unmount tail
  5. ExecutionFollowupController abort path / modal portal unmount
  6. runtime/executionWorkspaceWarm hot.dispose
  7. executionFilesEagerHydrate reduced-motion early return catch
  8. executionStateMachine degraded catch / chrono rollback
  9. executionSummonsWorkflow BFF push degraded failure catch
  10. (OPTIONAL 10th) Decisions cross-link requireDecisionsStorageExecutionId degraded path
- **Evidence Sources**: grep `EXECUTION_TEARDOWN_EVENT` = ≥2 hits (const + dispatch) + grep `tearDownExecutionFloatingState` = ≥9 occurrences + grep `data-closing` = ≥7 setAttribute hits + 9 principle grep checks each = ≥1 match + P3b ORDER check line-numbers P3 Ln < P3b Ln+1 < P4 Ln+2 (strictly ordered not just count)
- **Test Requirement**: Execution honesty family (existing 31 phase/probe tests + new tearDownExecutionFloatingState.test + executionNetworkAbortSafe.test → total tests ≥26 → exit code 0 all PASSED

#### AC-3 (rule): Perf Latest Mark ×2 Paths + restoreAllMocks + ≥4 Null Scenarios
- **Latest Mark Pattern**: (A) `ExecutionDashboard helpers executionArchive or Dashboard-level perf` → find actual perf helpers (if none exist inline helpers inside CP-11 State Machine chrono) THEN apply `entries[entries.length - 1]` latest entry (not first index [0] which causes stale reopen per CP-11 chronological graph) inside 2 OFFICIAL PATHS: Path1 execution perf (e.g. dashboard mount → interactive delta) + Path2 CP-11 chrono perf (state-transition last delta) OR Path1: runtime execution perf metric helper if exists
- **Negative Delta Null Guard** before every Math.round in both paths: `if (interactive.startTime < open.startTime) return null;` reversed time → null safe no negative
- **STRICT restoreAllMocks PLACEMENT NON-NEGOTIABLE**: grep `vi.restoreAllMocks` across ALL execution PERF test files EXACTLY 1 occurrence ONLY in primary execution perf test file; ZERO occurrences in ANY file that contains `vi.hoisted()` (CP-16 4× chunk scope tests use vi.hoisted heavily — restoreAllMocks NEVER inside hoisted-files)
- **beforeEach clearMarks ≥3 hits**: inside beforeEach ≥2 execution perf test files (primary + additional state-machine-chrono perf test if separate), `performance.clearMarks()` + `performance.clearMeasures()` wrappers or direct lines → count ≥3
- **≥4 Null Scenario it blocks**: 4 tests null minimum (A1 no marks at all) (A2 start only without end) (B1 reversed time negative delta → null before round) (B2 performance API degraded mode without getEntriesByName) optional B3 chrono-transition no-marks = 5 bonus
- **Evidence Sources**: grep `entries\[entries\.length - 1\]` ≥2 matches on 2 execution paths + restoreAllMocks exactly=1 in perf-files + 0 in hoisted files ≥3 clearMarks beforeEach lines + ≥4 `it('...null')` blocks
- **Test Requirement**: Primary perf test + chrono perf tests aggregated → total ≥8 tests exit 0 PASS

#### AC-4 (rule): Security 4 طبقات + WIFE BFF (0 supabase.from on CR-1..CR-8)
- **4 Layers**:
  - (L1 Navigation Whitelist): grep `window\.location\s*=|history\.push|location\.href\s*=` CR-1..CR-8 production excluding tests = 0 matches
  - (L2 Session Ownership Gate): `if (!userId) return;` + `EXECUTION_OWNERSHIP_GUARD` one-line comment in (أ) useExecutionCreationSubmit commit before writing (ب) executionDomainIsolationGates gate fn (ج) executionDecisionsNamespaceWrite FIRST LINE (د) ExecutionDossierRepository save/commit → ≥4 hits minimum CP-18 recommends ≥6
  - (L3 WIFE BFF): grep `supabase\.from\(` CR-1..CR-8 excluding tests **0 matches** (baseline A1 already FREE; re-verify only after tasks 1-3 no retroactively added client-supabase)
  - (L4 At-Rest SecureStore): `SecureStoreService.ensurePersistedReady()` FIRST LINE try/catch typeof-guard inside (أ) executionFilesStorage write (ب) ExecutionDossierRepository open (ج) executionDashboardStorePersist.load/save → ≥3 hits minimum CP-02 4-layer durability ideal ≥4
- **Permissions 12 canXxx Matrix NEW FILE**: Create `src/app/services/execution/executionPermissions.ts` exports 12 predicates readonly no any-casts (CP-12 12 isolation gates map 1-to-1 ideal here): canCreateExecution / canEditExecutionParties / canDeleteExecutionDraft / canAddSeizureOutcome / canApplySpecialFollowup / canRaiseExecutionAppeal / canManageExecutionFinancials / canIssueExecutionSummons / canAccessExecutionArchive / canModifyGuarantorDetails / canEvictTenantExecution / canDownloadExecutionFiles
- **Evidence Sources**: grep nav methods = 0 / ownership guard ≥4 hits / supabase.from = 0 / ensurePersistedReady ≥3 hits / permissions export count exactly 12 canXxx names
- **Test Requirement**: executionPermissions.test 12 predicates + existing domain isolation tests + storage tests + decisions tests aggregate → ≥40 tests exit 0

#### AC-5 (rule): XSS 5-Layer 2-Phase Strip + ≥2 inbound + ≥2 outbound sanitizeProfilePlainText (4 distinct files min ZVF no DOM edits)
- **Phase 1 Inbound L2/L3 Sanitize**: L2 Input Sanitize FIRST LINE mapper `sanitizeProfilePlainText()` (REAL NAME grep project first for actual existing import; use EXACT SAME NAME pattern Repository used successfully: `@/app/services/profile/profileUrlSanitize.ts` actual path `sanitizeProfilePlainText` function name — DO NOT INVENT NEW NAME) inside minimum 4 distinct inbound locations (A) useExecutionCreationSubmit party names write (B) useAlimonyCalculator debtor labels before calculation cache (C) executionDomainIsolationClaimModules before claim insertion (D) buildExecutionViewData before rendering party names → ≥2 inbound minimum 4 distinct files ideal
- **L3 Legal whitelist RegExp** on same 4 inbound call sites: Arabic/English/digits/spaces `[,\.()\-]` replace call
- **Phase 2 Outbound L4/L5 Network boundary**: First line payload sanitizeProfilePlainText BEFORE BFF network fetch call at ≥2 distinct boundaries (A) executionSummonsWorkflow payload.partyName → before summons hub BFF push (B) application execution followup hidden actions submit → before caseShare/BFF request → ≥2 distinct outbound call sites minimum 2 actual files
- **Total sanitizeProfilePlainText count ≥4 (≥2 inbound + ≥2 outbound) exact Repository precedent**: CR ≥4 grep hits + sanitize imports ≥4 distinct production files
- **Evidence Sources**: grep sanitizeProfilePlainText ≥4 hits total + ≥4 distinct production non-test files import it + ≥2 L3 whitelist regex calls
- **Test Requirement**: existing executionWorkFlow.test + inputSanitizer family tests pass no fail (if below compensated by existing sanitizer tests)

#### AC-6 (rule): Throw Opcode Prefix ≥95% Coverage [execution:<submod>:<opcode>] canonical 3-part format
- **Baseline A5 estimate**: N≈25 execution-scope actual throw sites (CP-07 24 validation files, CP-11 state-machine, CP-12 domain-gates, CP-18 decisions cross, CP-02 storage, CP-03 alimony analysis pipeline) → M=0 prefixed → target ≥95% so ≥24/25 actual = all 25 if possible
- **Prefix Convention 3-part snake case EXACT**: `[execution:<submod>:<VERB_opcode>]` examples: `[execution:submitValidation:debtor_share_sum_mismatch]`, `[execution:stateMachine:chronological_order_violation]`, `[execution:domainIsolation:cross_party_visibility_breach]`, `[execution:decisionsNamespace:write_userId_missing]`. Finite submods actual: submitValidation/stateMachine/domainIsolation/decisionsNs/filesStorage/alimonyAnaly/summons/financial/seizure/followup. No fabrication non-existent throws in zero-hit modules.
- **Evidence Sources**: grep `throw new Error\(` / `throw Error` CR-1..CR-8 production only. prefix match regex `\[execution:` count M divided by N total throws ≥ 0.95 rounded down. Raw ratio line displayed in completion evidence.
- **Test Requirement**: existing validation tests (validateExecutionCreationSubmit 3 split tests + debtorSplitConstraints tests + stateMachine chrono test + domain isolation tests) all pass exit 0 (no opcode-specific tests; grep evidence sufficient)

#### AC-7 (rule): Honesty Console Zero + DEV-only wrapping + Diagnostics 1st RUN (1/3) = literal []
- **Honesty ≥90%**: M wrapped / N pre-existing production console hits (A6 baseline ~3-4 pure execution) → ratio wrapped / original ≥ 0.90
- **Console Zero E1 FINAL**: grep CR-1..CR-8 production exclude tests console.* + debugger; COUNT = 0 EXACTLY
- **Wrap Convention**: `if (import.meta.env.DEV) { console.xxx(...originalArgsVerbatim); }` preserve arguments verbatim ZERO console argument edits; NOT process.env.NODE_ENV
- **First Diagnostics RUN (1/3 USER MANDATE)**: `GetDiagnostics` invocation returns EXACT LITERAL EMPTY ARRAY `[]` exact not "no issues visible"; if length>0 iteratively fix TS/enum/unused-vars until literal [] with zero hints zero warnings zero errors.
- **Evidence Sources**: console count grep = 0 + DEV wrappers ≥ baseline hits + Diagnostics raw json screenshot or raw copy line = `[]` literal.
- **Test Requirement**: execution honesty family ~31 tests pass 100% exit 0

#### AC-8 (rule): Mobile Safe-Area ≥8×4 dirs + EscapeStack 4-Priority REAL + Abort×3 Singletons + Diagnostics 2nd RUN (2/3) = literal []
- **L1 Safe-Area ≥8 additive hits × 4 directions top/bottom/left/right ALL ≥1 each**: (1) Find central theme execution file smartExecutionTheme.ts if exists OR create 6 additive env(safe-area-inset-*) tokens inside executionDashboardStaticChunkScope / ExecutionCreationFormVm glass Ui strings; PLUS reuse Repository precedent 4× pre-existing gallery global safe-area tokens to aggregate → total ≥12 (top4/bottom4/left2/right2) exactly pattern; NO component edits STRICT ZVF only central theme/chunk scope CSS strings additive
- **L2 EscapeStack REAL L0..L3 via RE-EXPORT backward compat zero caller edits T2 stub replacement pattern**: (L0 Modal First Responder) (L1 Overlay Dismiss) (L2 Fragment Back) (L3 Deep Navigation FollowupTabs) 4 priority; create real file executionEscapeStack.ts with push/pop/peek/unblockAll 4 functions + publicApi obj + SSR-safe window.__hamiExecEscapeStack side-effect boot; THEN in executionCloseEvents.ts DELETE OLD unblockAll stub lines REPLACE WITH 1 line `export { unblockAllExecutionOverlayEscape } from './executionEscapeStack';` — 0 caller edits 100% ZVF (Litigation T8 canonical precedent exact copy)
- **L3 Abort×3 Singletons + global attach typeof-guard**: NEW FILE executionNetworkAbort.ts 3 file-level singletons: (1) abortExecFilesHydrateAll (2) abortExecFinancialSyncAll (3) abortExecSummonsFollowupAll → paired 6 getSignal/abort fns + abortExecutionNetworkAll public + attachExecutionAbortGlobals populates ≥4 window globals (files/sync/summons/networkAll = 4) with typeof guard; + side-effect attach call at BOTTOM of file; + executionCloseEvents.ts TOP LINE `import './executionNetworkAbort';` side-effect hub boot singleton auto wire (EXACT Repository/Litigation T8 pattern)
- **Second Diagnostics RUN (2/3 USER MANDATE)**: GetDiagnostics literal exact empty array [] not just <2 items
- **Evidence Sources**: safe-area hits grep ≥8 ×4 directions = 32 pattern lines OR ≥12 aggregate (Repository 12/8 precedent) all 4 directions count ≥1 each; escape grep 4 funcs impl + 1 re-export 1 import side-effect = 6 lines yes/no; abort 3 singletons exports + 4 globals attach + 1 hub import = yes/no; Diagnostics = [] exact
- **Test Requirement**: existing escape/abort cross tests + execution mobile tests + 2 new files tests → total ≥30 tests exit 0 (if below compensated by existing honesty family 31 tests)

#### AC-9 (rule): Execution Gate Upgrade 4-Phase + EXEC_SHADOW_STUB adapted ANTI-MODULE-SHADOWING 4 WRONG SUBFOLDER PATHS
- **Phase 0 (ANTI-MODULE-SHADOWING BOMB EXEC_SHADOW_STUB)**: node:fs `globSync({ caseSensitive:false, nodir:true })` — NOT existsSync (Windows false positive per Repository Gate trap) on 4 INTENTIONALLY WRONG LOCATIONS execution MUST NOT exist ANY hit instant exit 1 FAIL:
  1. `src/app/components/Lawyer/execution-dossier-notes/ExecutionNucleusSheet.tsx` (wrong capital L Lawyer + wrong subfolder)
  2. `src/app/SERVICES/vault/VaultServices/executionOwnershipGate.ts` (wrong SERVICES caps + wrong subfolder)
  3. `src/app/HOOKS/lawyerDashboard/execution/executionShellLifecycle/executionOpenFlow.ts` (wrong HOOKS caps + fake lifecycle subfolder)
  4. `src/app/RUNTIME/storage/encryptedStorage/lawyerExecutionBootHydrator.ts` (wrong RUNTIME caps + wrong encrypted storage subfolder path)
  → REQUIRE ALL 4 return empty glob result 4/4 CLEAN; ≥1 FAIL → exit(1) BEFORE phase1 runs
- **Phase1 Critical Paths Discovery REAL disk not hardcoded**: ~18 criticalGlobs (CR-1..CR-8) dedup with globSync actual real → matched paths count ≥56
- **Phase2 vitest ≥237 tests across ≥40 execution test files dual runs**: Windows CMD ROBUSTNESS HARDENED Repository precedent: (a) FIRST use globSync INSIDE gate to pre-expand actual real execution test file paths array INDIVIDUAL PATHS not brace-expansion glob patterns (which Windows CMD ignores 100%); verify total count ≤ ~90 files safe ≤32KB line limit (~8KB safe); then pass those already-expanded individual file paths to vitest directly; TWO SEPARATE SPAWN_RUNS with SPAWN_OPTS {shell:true, maxBuffer:500*1024*1024, timeout:600000ms, windowsHide:true}: (A) verbose run tests-passed-summary count ≥237 (B) JSON reporter parse JSON.parse(result.stdout).testResults → length ≥40 files AND .numTotalTests ≥237 AND numFailedTests=0 AND every single testResult status==="passed" AND regression ratio passed/total ≥0.99
- **Phase3 PASSED banner exact**: LAST stdout line STRING EXACT equality `=== "=== Gate result === PASSED"` (no typos no extra spaces exact match tasks.md L420 litigation/repo precedent) + stderr final last line EXACT banner `'===== EXECUTION TIER-1 PRODUCTION GATE PASSED ====='` BEFORE process.exit(0) only after both stdout+stderr banners written
- **Create/Upgrade**: Upgrade `scripts/execution-production-gate.mjs` (existing primary gate) from current version → 4P STRICT implementing above phases. Sibling gates fast/probes/manifest untouched to reduce risk
- **Evidence Sources**: Gate raw stdout+stderr with phase0-4 banners printed sequentially + phase0 4/4 CLEAN + phase1 paths≥56 + phase2 verbose≥237 + json files≥40 tests≥237 all-pass ratio≥99% + phase3 last-line exact match + exit code=0 EXACTLY
- **Test Requirement**: run node scripts/execution-production-gate.mjs once → all pass exit0 (gate self-test integrated)

#### AC-10 (rule): Honesty + Dual Guard + Opcode + XSS Boundary Regression ≥99% No-Fail on existing 200+ execution test family
- Entire execution test family CR-1..CR-8 aggregate vitest run single aggregate. Total pass ratio M/N ≥ 0.99 (fail ≤1 allowed only with user explicit approval; default expected 0 failures)
- **Evidence Sources**: vitest JSON reporter total N passed M ratio displayed numeric ≥0.99
- **Test Requirement**: actual aggregate run; pass/fail ratio-based

#### AC-11 (rule): Final Diagnostics RUN 3/3 USER MANDATE = literal [] exactly + 0 pending items = 0
- GetDiagnostics literal `[]` exact json empty array length property 0
- All 10 tasks tasks.md Status ∈ {completed, user-approved cancelled only} → 0 items ∈ pending, in_progress, blocked
- Evidence: Diagnostics raw = [] + grep tasks.md 10× completed lines

---

### Rubric-Type AC (3 Rubrics — evaluative with numeric scale + explicit pass threshold)

#### AC-12 (rubric: Lifecycle Clarity / Session + Close Freshness across CP-01 8 Surfaces)
- **Dimension**: وضوح دورة حياة التنفيذ + نضارة الجلسة + صحة الإغلاق الجراحي عبر 8 الأسطح الرسمية (أكبر قسم حتى الآن)
- **Scale**: 0-5
- **Anchors**: 1=لا حراس جلسة أبدًا؛ إغلاق مبعثر بدون دالة مركزية tearDown; 3=حراس جلسة موجودة ≥2 هوكات لكن ≤12 dual guard فقط؛ دالة tearDown موجودة لكن <6 مواقع استدعاء؛ ضبابية في Placement Rule; 5=4+ هوكات × dual guard ≥26 (26+) + Placement Rule صحيح reset inside cleanup فقط 4 hits / 0 outside + tearDown 9 مبادئ مرتبة STRICT + P3b order checked + ≥9 call sites (≥10 ideal) + 17+ transient keys actually deleted (≥39 ideal CP-05)
- **Pass Threshold**: ≥4/5
- **Evidence Sources**: CR grep counts (8 counters /26 guards /9 tearDown /7 data-closing attr /≥39 window keys) + 4 session contexts test files exit=0

#### AC-13 (rubric: Production Hardening Readiness — Security / Perf Budget / Mobile / Opcode / Zero Console)
- **Dimension**: جاهزية قسم التنفيذ الأعقد للانتاجية الفعلية على هواتف المستخدمين واجهات سطح المكتب
- **Scale**: 0-5
- **Anchors**: 1=ثغرات واضحة: استدعاء مباشر supabase في الكلاينت، XSS غير محمي، console ملوث، لا safe-area في الموبايل; 3=أغلب الطبقات موجودة لكن ثغرات طفيفة: WIFE=0✅ لكن ≤2 outbound sanitize فقط، console=0 لكن <95% opcode prefix فقط، ≤4 safe-area hits فقط; 5=WIFE BFF=0✅ + XSS inbound≥2 + outbound≥2 (≥4 distinct files) ✅ + Opcode≥95% (24+/25+) ✅ + Console=0 ✅ + Safe-area 12 total top4/bottom4/left2/right2 ≥1 each direction ✅ + Abort×3 singletons + 4 globals + hub side-effect ✅ + EscapeStack L0-L3 real + RE-EXPORT zero edits ✅ + Perf latest-mark ×2 paths + restoreAllMocks STRICT 1-location no hoisted-files ✅
- **Pass Threshold**: ≥4/5
- **Evidence Sources**: A1..A7 baselines after tasks + tasks 4-8 completion evidence numeric tables + 3× Diagnostics all = []

#### AC-14 (rubric: Honesty / Clean Build / Zero Mutation Side Effects Observed During Gate Run)
- **Dimension**: الصدق والشفافية في إغلاق قسم التنفيذ الأعقد؛ عدم وجود آثار جانبية للحفظ والتحويلات على الحالة العامة للمشروع خارج نطاق التنفيذ؛ نقاء الـ Build والاختبارات
- **Scale**: 0-5
- **Anchors**: 1=Build مكسور أو اختبارات كثيرة تفشل؛ آثار جانبية تؤثر على 3+ أقسام أخرى (مستودع/دعاوى/تقويم/منتدى); 3=جميع اختبارات التنفيذ تمر لكن ≤3 اختبارات الأقسام الأخرى فشلت بعد تعديلات التنفيذ؛ أو ≤2 تحذيرات TypeScript; 5=100% tests pass (≥237 execution, ≥40 files) + 8 أقسام مغلقة سابقاً (الإعدادات/البحث/الإشعارات/الملف/المهام/المنتدى/التقويم/الدعاوى/المستودع=9) جميعهم تمر 100% بدون تراجع + GetDiagnostics ثلاث مرات [] تماماً + مشاكل برمجية=0 + Console جذور الإنتاج التنفيذ=0
- **Pass Threshold**: ≥4/5
- **Evidence Sources**: Gate Phase2 exit=0 + PASSED banner exact last line + TypeScript/tsc --noEmit clean exit 0 + 3× Diagnostics literal = [] + optional regression 8 sections re-run pass ratio

---

## End of Execution spec.md — Total ACs: 14 (11 rule + 3 rubric) / Total E closure conditions ×2 (E1 Console=0 + E2 Diag 3×=[])
